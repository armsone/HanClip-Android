# HanClip Android 프로젝트 개발 규칙

이 문서는 HanClip Android를 개발하면서 사람과 Codex가 공통으로 지켜야 할 규칙이다.

## 1. 작업 시작과 범위

- 작업을 시작하기 전에 짧은 계획을 먼저 보여준다.
- 불필요하거나 반복적인 작업을 줄여 Codex 주간 사용량과 크레딧을 아낀다.
- 이 저장소 밖의 파일을 수정하거나 삭제하지 않는다. 특히 형제 경로의 HanClip iOS 저장소는 읽기 전용 기준으로만 사용한다.
- 사용자가 만든 변경과 현재 작업에 관계없는 변경은 되돌리거나 덮어쓰지 않는다.
- 요구사항이 불명확하면 기존 동작과 사용자 데이터를 보존한다.
- 사용자 답이 있어야 진행할 수 있으면 메시지 첫 줄에 `답변 필요:`를 명시한다.

## 2. 독립 프로젝트 원칙

- 이 저장소는 iOS 저장소 없이도 clone 후 Gradle 빌드가 되어야 한다.
- 기존 HanClip iOS 저장소 `/Users/armsone/git/HanClip`과 프로젝트 안의 `ios-source-readonly` 링크는 비교·분석을 위한 읽기 전용 자료다.
- Codex는 기존 HanClip iOS 저장소에서 파일 생성, 수정, 삭제, 이동, 이름 변경, 포맷팅, 빌드 산출물 생성, Git stage·commit·push를 절대로 수행하지 않는다.
- iOS 기준 갱신이 필요해도 기존 HanClip 폴더는 읽기만 하고, 필요한 스냅샷은 이 Android 저장소의 `reference/ios-current` 안에서만 갱신한다.
- 빌드가 형제 폴더나 개인 Mac의 절대 경로에서 폰트·이미지·설정을 가져오지 않게 한다.
- Android 빌드 자산은 `app/src/main/assets` 또는 Android 리소스 폴더 안에서 관리한다.
- iOS 비교 기준은 `reference/ios-current`에 복사된 고정 자료를 사용한다. 기준 갱신은 현재 작업이 iOS 동등성 비교를 요구하거나 사용자가 명시적으로 요청한 경우에만 작업 시작 시 한 번 수행한다.

## 3. 수정과 검증

- 소스를 수정하면 컴파일 오류를 먼저 해결하고 `:app:assembleDebug`와 변경에 직접 관련된 테스트를 실행한다. `:app:lintDebug`와 전체 테스트는 배포 후보, 공통 기반·외부 의존성 변경, 영향 범위가 불명확한 변경 또는 사용자 요청 시에 실행한다.
- 화면 변경은 표시, 터치 범위, 상태 전환, 접힘·펼침·회전을 함께 확인한다.
- 실기기 검증은 사용자가 별도로 허용하지 않는 한 `SM-F968N`만 사용하고 `SM-T500`은 사용하지 않는다.
- 설치는 기본적으로 `adb install -r`로 사용자 데이터를 보존한다.
- 일반적인 소스 변경은 로컬 수정과 위험도에 맞는 검증까지 수행한다. `SM-F968N` 설치·확인은 사용자가 이번 작업에서 명시적으로 요청한 경우에만 수행한다.
- 설치 요청은 커밋·푸시를 포함하지 않는다. 초기 설치 상태 검증은 설치 횟수가 아니라 최초 설치·마이그레이션·초기값 영향이 있거나 사용자가 요청한 경우에만 별도 승인 후 수행한다.
- 반복 수정·설치에서는 실패했거나 변경과 관련된 검증만 다시 실행하고, 이미 통과한 전체 검증을 이유 없이 반복하지 않는다.
- Mac과 폰이 `zangzip`에 연결되어 있으면 무선 디버깅을 우선한다.
- 실기기 작업 중에는 충전 중 화면 켜짐을 유지하되 잠금 자격 증명을 추측하거나 입력하지 않는다.
- 작은 수정마다 Gradle 빌드·테스트하지 않고, 컴파일과 검증이 가능한 하나의 의미 있는 변경 묶음이 완성된 뒤 검증한다.
- 변경 전 기준 테스트는 버그 재현, 현재 기준 상태가 불명확한 경우 또는 공통 기반 변경에만 실행한다. 같은 소스 상태·Gradle task·환경에서 통과한 검증은 반복하지 않는다.
- 검증은 변경 관련 JVM 테스트, `assembleDebug`, 필요한 lint·기기 확인 순서로 최소 범위부터 넓힌다. 전체 테스트와 lint는 배포 후보에서 한 번만 실행한다.
- 같은 Gradle·ADB·기기 인프라 오류가 두 번 반복되면 같은 명령을 다시 실행하지 않고 원인을 진단한다. 실패 수정 후에는 실패 task와 직접 영향 범위만 다시 확인한다.
- 여러 에이전트나 작업이 같은 Gradle daemon, 에뮬레이터, AVD 또는 실기기를 동시에 사용하지 않는다. Gradle worker와 병렬 실행은 명확한 시간 이점과 충분한 메모리가 확인된 경우에만 제한적으로 사용한다.
- 빌드·테스트 전 데이터 볼륨의 여유 공간을 확인한다. 50GiB 미만이거나 전체 용량의 10% 미만이면 새 대형 빌드·전체 테스트·에뮬레이터 생성을 시작하지 않는다.
- 작업 전용 빌드 디렉터리·테스트 결과·임시 파일은 성공·실패·중단 후 더 이상 필요 없으면 정리한다. 공용 Gradle 캐시와 다른 프로젝트 산출물은 임의로 삭제하지 않는다.
- Mac의 디스크, 메모리, CPU, 배터리, 스왑, Gradle daemon, 에뮬레이터와 연결 기기는 사용자 자원으로 보호한다. 장시간 고부하와 무제한 프로세스·AVD·로그·캐시 누적을 허용하지 않는다.
- 진행 확인이나 상태 보고만을 위해 빌드·테스트·설치를 실행하지 않고 이미 확인한 결과를 재사용한다.

## 4. 버전과 Git

- 현재 공통 기준 `versionName`은 `2.2.1`이다. 중요 수정은 minor, 작은 수정은 patch를 올리고 major는 사용자의 직접 지시가 있을 때만 변경한다.
- 릴리스 후보의 `versionCode`는 2026-01-01 00:00 KST 이후 지난 분 수와 직전 값+1 중 큰 값으로 단조 증가시키고, 화면에는 후보 생성 시 고정한 `YYYYMMDDHHMM` 표시 빌드를 사용한다.
- 이 Android 프로젝트의 GitHub 게시 대상은 공개 저장소 `armsone/HanClip-Android`로 고정하고, 새 원격 `origin`도 이 저장소만 사용한다.
- 과거 통합 프로젝트와 기존 GitHub 업로드는 백업으로만 취급하며 새 Android 변경을 푸시하지 않는다.
- 수정 횟수나 변경 규모만으로 자동 커밋·푸시하지 않는다. 사용자가 이번 작업에서 명시적으로 요청한 경우에만 검증된 의미 있는 변경 단위로 수행한다.
- 푸시 전 현재 브랜치, 원격 변경, 충돌 가능성을 확인하며 강제 푸시는 사전 확인 없이 하지 않는다.

## 5. iOS 동등성

- iOS 동등성 작업 사이클을 시작할 때만 `reference/ios-current/design-spec`의 관련 최신 명세를 읽고 기준 시점을 한 번 고정한다. 현재 요청과 무관한 명세 전체를 관행적으로 다시 읽지 않는다.
- iOS 동등성 작업은 시작 시 기준을 한 번 고정하고 작업 도중 반복 조회하지 않는다. 일반 Android 작업에서는 iOS 변경을 관행적으로 확인하지 않는다.
- 텍스트, 아이콘, 핵심 동작, 수치, 데이터 구조는 iOS 기준에 맞춘다.
- Android의 넓은 화면, 성능, 접근성은 기능 의미를 바꾸지 않는 범위에서 개선한다.
- iOS에만 있는 항목과 바로 이식할 수 없는 항목은 `IOS_PARITY.md`에 차이, 이유, 후속 작업을 기록한다.
- 카드, 버튼, 입력창, 팝업과 플로팅 패널은 둥근 사각형을 기본으로 하고 패널은 불투명 테마색, 16dp 이상 모서리, 얇은 테두리를 사용한다.
- 바형 폰을 기본으로 하되 600dp 이상에서는 프리셋·컬렉션 열 증가나 좌우 분할로 넓은 화면을 활용한다.
- 접힘·펼침·회전 중 선택 상태와 편집 내용을 보존한다.

## 6. 배포 APK

- 사용자가 바탕화면 외부 설치 APK 생성을 명시적으로 요청한 경우에만 `HanClip-MMDD-HHMM.apk` 형식을 사용한다.
- 기존 APK 삭제는 자동으로 수행하지 않는다. 사용자가 정리를 요청하면 정확한 대상과 복구 불가능성을 확인한 뒤 진행한다.

## 7. 카피라이터 기능 사전

- 기능을 추가하거나 명칭·핵심 동작을 바꾸면 카피라이터 정보창의 기능 사전도 함께 갱신한다.
- 제거되거나 이름이 바뀐 기능의 오래된 설명을 남겨두지 않는다.

## 8. `N%로 작업 시작해`

- `N`은 이번 작업에 허용된 주간 사용량 퍼센트포인트다.
- CCMB의 남은 주간 사용량과 크레딧을 조회할 때는 `/Users/armsone/.codex/bin/ccmb-usage`를 즉시 실행한다. 별도의 임시 `codex app-server` 클라이언트를 만들거나 웹·화면 수치로 대신하지 않는다.
- 명령의 JSON에서 `weeklyRemainingPercent`, `origin`, `fresh`를 확인한다. 사용자가 퍼센트로 작업을 지시한 경우 크레딧은 보고하지 않는다. `fresh`가 `false`이면 오래된 데이터라는 사실만 짧게 알린다.
- 시작 시 CCMB의 주간 사용률을 기록하고 의미 있는 작업 묶음 사이에 다시 확인한다.
- 허용량에 도달하면 새 작업을 시작하지 않고 안전하게 마무리한다. 반영 지연과 정수 반올림으로 정확히 일치하지 않을 수 있다.
- 사이클이 끝나도 허용량이 남으면 고정된 iOS 기준과 `IOS_PARITY.md`의 다음 안전한 우선순위를 자동으로 이어간다.
- 사용자가 `이제 끝내`라고 하면 진행 중인 사이클의 정리와 검증까지만 마치고 멈춘다.
- 중간 보고는 간헐적으로 `허용 N% 중 R% 남음` 형식 한 줄로만 표시한다.
- 완료 시 시작·종료 사용량, 실제 증가분, iOS 동등성, 남은 예상 날짜와 추가 사용량을 보고한다.

## 9. 완료 보고

- 수정 핵심, 빌드·린트 결과, 설치·실행 결과, 커밋·푸시 여부를 간단히 보고한다.
- 앱 삭제, 데이터 초기화, 강제 푸시처럼 복구가 어려운 작업은 결과와 복구 가능 여부를 알린다.
