# HanClip iOS → Android 동등성 추적

이 문서는 Android를 현재 iOS 구현에 맞추는 장기 작업의 기준표다. 전체 50개 항목을
각 2점으로 평가한다. 소스와 실기기에서 확인된 동일 동작은 2점, 핵심 흐름은 있으나
세부 수치·상태·화면이 다른 항목은 1점, 확인되지 않았거나 없는 항목은 0점이다.

## 일일 iOS 기준

| 확인일 | iOS 기준 커밋 | 확인 결과 |
| --- | --- | --- |
| 2026-08-09 | `ef68f4e5` | 대규모 변경 뒤 기능·모델·저장·편집·컬렉션·Quick/Life·엔딩 카드 구조 재조사 |
| 2026-08-09 22:17 KST | `3d4414bc` + iOS 작업 트리 고정 스냅샷 | 이번 5% 작업 기준. Quick 전체 화면 설정, 사진 선택 재선택 동기화, 3종 홈 빠른 실행, 여행 엔딩 선택형 변경을 확인했으며 작업 묶음 종료 전에는 다시 읽지 않음 |
| 2026-08-09 23:12 KST | `49ccf934` | 이번 야간 작업 고정 기준. 컬렉션 20개 제한·핀 정렬, 접히는 클립 설정·프리셋 표시, 음악 아래 독립 엔딩 행·위치 없이 사전 설정, Quick 엔딩 진입을 확인했으며 오전 6시까지 다시 읽지 않음 |
| 2026-08-10 | `88735597` + iOS 작업 트리 SHA-256 `c976c87b…b18513` | 이번 5% 작업 고정 기준. 커밋되지 않은 최신 iOS 컬렉션 압축·메타데이터 순서·전용 플레이어 변경까지 포함해 전체 Swift/Kotlin 기능 표면을 다시 대조 |
| 2026-08-10 13:16 KST | `da55f9a9` + `EditorView.swift` `4f9161ec…4410` + `MovieCollectionStore.swift` `6ac28393…dfe1` | 사진·달력 재구축, Google Photos 전용 경로 제거, 컬렉션 개별/일괄 압축, 전용 플레이어 확대·이동·컨트롤 변경까지 다시 고정해 Android 빌드 518과 대조 |
| 2026-08-13 22:19 KST | `31e60ec5` + iOS 최종 미커밋 작업 트리, 빌드 `1.0.1 (3.11.44)` | `ANDROID_FULL_PORT_SPEC_2026-08-13.md`로 오늘 변경과 기존 명세의 구형 항목을 전수 고정. Android 소스 반영 여부는 아직 재감사·미검증이며 완료로 계산하지 않음 |

Android 작업이 다음 날까지 이어지면 그날 첫 소스 변경 전에 iOS 커밋과 변경 파일을
다시 확인하고 이 표를 갱신한다.

## 전체 화면 누락 점검 원칙

매일 iOS 기준을 확인할 때 아래 다섯 범주를 화면별로 함께 비교한다.

1. 텍스트: 제목, 설명, 상태 문구, 저작권·라이선스 고지와 항목 순서
2. 아이콘: 공통 정보 아이콘으로 대체하지 않고 항목 의미에 맞는 개별 아이콘
3. 기능: 탭·길게 누르기·선택·저장·복원·삭제·공유와 상태 유지
4. 페이지: iOS에 존재하지만 Android에서 진입할 수 없거나 빠진 화면
5. 디자인: 배치, 간격, 크기, 색상, 테두리, 그라데이션, 펼침·접힘과 빈 상태

확인하지 않은 항목은 완료로 계산하지 않는다. 즉시 이식할 수 없는 차이는 아래 표에
남기고 완료 보고에서 누락 항목, 이유와 후속 작업을 함께 알린다.

| 발견일 | 화면 | 범주 | 차이 | 상태·후속 작업 |
| --- | --- | --- | --- | --- |
| 2026-08-09 | 카피라이터 | 텍스트·아이콘 | `외부 음악`·`내장 서체 저작권` 고지 누락, 여러 항목이 동일 정보 아이콘 사용 | Android 빌드 440에서 원문 고지와 항목별 아이콘 복원 |
| 2026-08-09 | 음악 브라우저 | 기능·버튼 | 북마크 롱터치 등록/해제와 명시적인 즐겨찾기 관리 진입이 누락됨 | Android 빌드 442에서 짧은 탭 목록, 롱터치 등록/해제, 관리 화면·전체삭제·파일 저장·순서 변경 복원 및 실기기 검증 |
| 2026-08-09 | 음악 브라우저 | 디자인 | 즐겨찾기가 화면을 미는 고정 영역이고 iOS식 플로팅 패널·항목 증가 높이·초과 스크롤 표시가 없음 | Android 빌드 444에서 좌우 12dp 여백의 불투명 플로팅 패널, 항목 수 기반 높이, 화면 높이 제한과 스크롤바, 명시적 닫기와 뒤로가기 우선 닫기 적용. 4개 항목 실기기 노출 확인 |
| 2026-08-09 | 홈 테마 선택·공통 패널 | 디자인 | 로고 롱터치 테마 패널의 투명 배경 때문에 내용 판독이 어려우며 일부 카드·대화상자 모서리가 iOS보다 각짐 | Android 빌드 443에서 테마별 불투명 패널색과 공통 8/12/16/22/28dp 모서리 체계를 추가하고, 앱 내 카드·버튼·입력창·대화상자·플로팅 패널을 전수 정리. 전체 화면·원본 미디어·달력 격자만 예외로 유지, 테마 패널 실기기 확인 |
| 2026-08-09 | 홈 로고·카피라이터 버튼 | 기능·디자인 | 로고 짧은 탭도 선택 패널을 열어 순차 테마 변경이 사라졌고, i 버튼은 블러 없는 36% 반투명색이라 뒤쪽 내용이 어색하게 비침 | Android 빌드 445에서 로고 짧은 탭 테마 순환·롱터치 선택 패널을 분리하고 저장까지 실기기 검증. i 버튼은 테마 패널색 위에 보조색을 합성한 완전 불투명색과 7dp 그림자로 교체 |
| 2026-08-09 | 앱 시작·AiShot | 기능 | 처리한 AiShot 빠른 실행 Intent와 마지막 탐색 화면이 남아 일반 앱 아이콘 실행에서도 카메라가 다시 열릴 수 있음 | Android 빌드 446에서 빠른 실행 Intent를 1회 처리 후 중립 Intent로 교체하고, 단일 Activity 재실행 시 일반 런처 Intent를 홈 열기로 처리. AiShot 백그라운드→일반 앱 아이콘 실행 시 첫 화면 복귀 실기기 검증 |
| 2026-08-09 | 홈 컬렉션 | 페이지·디자인·데이터 | Android의 `완성 MP4` 별도 영역이 iOS의 새 `컬렉션` 설계 이전 구조 | Android 빌드 447에서 독립 컬렉션 저장소·포스터·책장형 목록·추가/이름변경/공유/삭제와 기존 완성본 무손실 이관 구현. iOS 세부 화면 수치는 후속 실기기 비교 필요 |
| 2026-08-09 | 달력 미디어 선택 | 디자인 | 날짜별 선택 썸네일 구분은 iOS에서 작업 예정 | Android 선행 구현 취소. iOS 완료 뒤 비교 이식 |
| 2026-08-09 | 홈 프리셋·Quick | 기능·데이터 | Quick·Life 프리셋과 Quick 목표 길이 흐름 누락 | Android 빌드 447에서 6종 프리셋, Quick 미디어 수 기반 권장 길이·최소 0.2초·기본 음악과 Life 기본값 구현. Quick 1개/1초 MP4 실기기 생성·컬렉션 자동 등록 확인 |
| 2026-08-09 | 컬렉션 가져오기 | 기능 | 사진/파일 선택, 진행 상태와 취소 가능한 원본 복사 흐름 누락 | Android 빌드 447에서 사진·파일 다중 선택, 동영상 판별, 순차 원본 복사, 실패 정리, 진행률·취소를 구현. 편집기의 통합 선택기와 전체 중복 방지는 후속 작업 |
| 2026-08-09 | MP4 정보 | 데이터 | 컬렉션 카드에 제작일·촬영기간·위치·길이 정보가 없음 | MP4 duration/date/location, 사진 EXIF·영상 GPS, 역지오코딩, 표준 timestamp/location과 `HANCLIP_METADATA` 기록·복원 구현. 위치 없는 EXIF의 0,0 값은 무효 처리 |
| 2026-08-09 | 엔딩 카드 | 기능·디자인 | iOS의 5종 엔딩 카드와 영상 합성 누락 | 자막·보물지도·여행일정·랜드마크·오피스 설정/미리보기/저장과 마지막 사진 클립 합성 구현. 여행 프리셋 보물지도 2초 기본값을 실제 3초 MP4 마지막 프레임에서 확인 |
| 2026-08-09 | Quick 전체 화면 | 기능·디자인 | iOS는 시간 프리셋·자막·음악·엔딩·화면비·사진/파일 추가를 한 화면에서 다루지만 Android는 길이만 조절하는 작은 팝업 | Android 빌드 451에서 전체 화면 퀵 패널, 8종 시간 선택과 최대 미디어 수, 5초 증감, 설정 요약/진입, 한 줄 화면비, 사진·파일 메뉴를 구현. 엔딩 행은 엔딩 설정 위치로 바로 이동 |
| 2026-08-09 | 앱 빠른 실행 | 기능·아이콘 | iOS 홈 빠른 실행이 사진·퀵모드·AiShot 3종으로 변경됐으나 Android 런처 바로가기가 없음 | Android 빌드 451에 의미별 아이콘을 가진 3종 정적 바로가기와 `hanclip://quick` 라우팅 구현. 시스템 등록 3건과 Quick 콜드 실행을 실기기 확인 |
| 2026-08-09 | 사진 재선택 | 기능·데이터 | iOS는 이전 사진 자산 식별자를 재선택 상태로 열고 사용자가 선택 해제한 클립을 프로젝트에서도 제거함 | Android 빌드 451에서 영속 원본 URI 기반 선선택, 현재 필터 범위의 안전한 해제 동기화, 빈 선택 적용, 그룹/영상 자식 정리와 되돌리기를 구현. 같은 세션에서 1번 선선택→해제→클립 1개 제외를 실기기 확인 |
| 2026-08-09 | 여행 프리셋 엔딩 | 기능 | 최신 iOS는 보물지도 테마만 기본 지정하고 엔딩 자체는 사용자가 켜지만 Android는 자동으로 켬 | Android 빌드 451에서 자동 활성화를 제거하고 테마 기본값만 유지 |
| 2026-08-09 | 자막·음악 전체 화면 | 기능·디자인 | 테마의 반투명 패널색 때문에 편집 화면이 뒤로 비쳐 글자와 버튼이 겹치고, 긴 설명이 닫기·워터마크 스위치를 오른쪽 밖으로 밀어내며 iOS 상단 되돌리기 버튼도 누락 | 빌드 455에서 완전 불투명 바탕+테마 배경, 자막 초깃값·음악 진입값 복원, 왼쪽 원형 되돌리기·오른쪽 원형 닫기·하단 우측 화면명 공통 헤더를 구현. 자막 헤더 3요소를 실기기 확인하고 영상 구간·사진 시간·달력·브라우저 전체 화면의 불투명 바탕도 통일 |
| 2026-08-09 | 컬렉션 핀·제한 | 기능·데이터·디자인 | 최신 iOS는 컬렉션 최대 20개, 핀 우선/최신순 정렬, 포스터 상단 직접 핀을 제공하지만 Android는 무제한·최신순이며 핀이 없음 | 빌드 457에서 기존 JSON의 핀 누락을 `false`로 복원하는 스키마 호환, 20개 저장 제한, 핀 우선 정렬, 상단 18dp 핀 구멍/빨간 핀과 메뉴 핀, 왼쪽 `현재/20`·오른쪽 컬렉션 제목을 구현 |
| 2026-08-09 | 클립 설정·엔딩 분리 | 기능·디자인 | 최신 iOS는 클립 설정을 기본 접힘으로 열고 시작 프리셋을 표시하며 엔딩을 음악 아래 독립 행/화면으로 제공하고 위치가 없어도 사전 설정 가능 | 빌드 458에서 접기/펼치기와 프리셋 배지, 엔딩 테마·0.5초 시간·사용 상태 독립 행, 사용 여부와 무관하게 5테마·시간·샘플 미리보기·자막형 15프리셋을 편집하는 독립 전체 화면, Quick 복귀 흐름을 구현 |
| 2026-08-09 | 영화 목록 프리셋 표식 | 기능·아이콘·디자인 | 최신 iOS는 영화 목록 왼쪽에 `현재/10`, 오른쪽 제목을 두고 각 저장 영화의 클립 수와 시간 사이에 시작 프리셋 아이콘을 표시 | 빌드 459에서 동일한 제목줄 구조와 6종 프리셋별 아이콘을 Android 편집 프로젝트 행에 반영 |
| 2026-08-09 | 엔딩 일정·테마 렌더링 | 기능·메타데이터·디자인 | Android는 같은 장소를 날짜와 무관하게 한 번만 표시하고 5테마가 사실상 색상만 달라 iOS의 날짜별 일정·차량/항공·테마 의미가 누락 | 빌드 460에서 연속 장소+촬영일 기준 일정 분리, 좌표 국가 판별과 차량/항공 표식, 자막형 글자/그림자색 승계, 여행일정 표·랜드마크 카드·오피스 보고서·보물지도 경로 렌더링을 분리 구현 |
| 2026-08-09 | MP4 이동 경로 메타데이터 | 데이터·상호운용 | Android는 첫 대표 장소만 저장해 iOS의 촬영 장소 이동 순서와 `HANCLIP_METADATA:` 스키마를 보존하지 못함 | 빌드 462에서 날짜+장소 기준 경로 배열과 `서울 → 부산` 표시를 저장하고 iOS 호환 설명 메타데이터 기록·ISO-8601 촬영기간/경로 읽기를 추가 |
| 2026-08-09 | Quick 영상 길이 | 디자인·상태 | Android 시간 스테퍼가 별도 큰 카드이고 추천시간과 같은 숫자의 일반 선택이 동시에 선택될 수 있으며 하단 만들기 버튼 높이가 다름 | 빌드 463에서 iOS 51dp 캡슐 스테퍼, 12dp 선택 간격, 추천/직접 선택 상태 분리, 14dp 설정 패널과 93dp 만들기 캡슐로 일치 |
| 2026-08-09 | 엔딩 설정 미리보기 | 디자인·버튼 | 실제 MP4 렌더러는 5개 테마가 분리됐지만 설정 화면 미리보기는 색상만 바뀌는 같은 장소 목록이고 엔딩 설정 진입 터치 영역이 제목 62dp에 한정됨 | 빌드 465에서 자막 이동선, 보물지도 점선 경로, 실제 날짜 여행일정, 지역 랜드마크 카드, 문서번호·이동수단 오피스 표로 미리보기를 분리하고 제어 버튼 외 행 전체로 설정 진입 영역 확대 |
| 2026-08-10 | 카피라이터 아이콘 | 아이콘·디자인 | 카피라이터 설정 제목이 영화 프리셋 격자 아이콘이고 `로고` 설명도 일반 하트 아이콘이라 iOS의 명함·실제 앱 심볼 의미와 다름 | 고정 iOS 기준을 유지한 빌드 466에서 제목을 명함 아이콘, 로고 설명을 원본 `logo_mark` 이미지로 교체하고 외부 음악·내장 서체 저작권 하단 표시 재검증 |
| 2026-08-10 | 음악 전체 화면 | 디자인·상태 | 선택된 내장 샘플을 상단에서 중복 미리보기하고 iOS에 없는 소리 믹스 요약·반복 카드·초기화 버튼이 추가돼 패널 순서와 높이가 다름 | 빌드 467에서 내장 샘플 중복 미리보기와 부가 카드를 제거하고 8dp 선택 패널, 66dp 샘플, 42dp 브라우저/파일, 통합 음량·페이드 패널로 고정 iOS 수치와 순서 일치 |
| 2026-08-10 | 자막 전체 화면 | 구조·디자인 | 예전 Android 자막 창에 카피라이터 워터마크 편집·상태 요약·골프 전용 버튼이 섞이고 프리셋·크기·간격이 독립 칩으로 길게 나열됨 | 빌드 468에서 워터마크 편집은 카피라이터에만 남기고 사용/안함 캡슐, 직접 미리보기 입력, 15종 3열 프리셋, 접는 전체 서체, 통합 크기·줄간격·색상·그림자 패널로 고정 iOS 구조에 맞춤 |
| 2026-08-10 | MP4 경로 메타데이터 | 데이터·호환성 | 실제 빌드 468 MP4에서 `routeLocationNames`가 JSON 배열이 아닌 문자열로 기록돼 iOS `Codable`이 전체 HanClip 메타데이터를 거부하고, 유효하지 않은 0,0 위치도 포함됨 | 빌드 469에서 `JSONArray`로 기록하고 유효 위치만 대표·경로에 포함. 같은 1초 MP4를 다시 생성·사진 앱 저장·추출해 배열과 null 위치를 확인하고 iOS 동일 Swift 스키마 디코딩 성공 |
| 2026-08-10 | 5종 엔딩 실제 렌더러 | 디자인·합성 | Android 보물지도는 세로 직선과 마지막 X, 랜드마크는 공통 막대 실루엣, 오피스는 영어 제목 중복으로 iOS의 곡선 지도·지역별 명소·보고서 의미와 다름 | 빌드 470에서 variation 곡선 점선·첫 지점 X·이동수단, 지도 안쪽 점선/나침반, 일정표 한영 헤더, 국내외 지역별 명소 그림문자, 여행 기록 보고서·문서번호·기간 구조로 동기화 |
| 2026-08-10 | 통합 미디어 선택 | 기능·디자인·성능 | Android 기본 사진첩은 한 달 단위로 성능을 보호하면서도 월 이동 UI가 없어 iOS 전체 보관함의 날짜별 탐색 범위를 재현하지 못하고, 월을 바꾸면 기존 선택 미리보기도 사라짐 | 빌드 470에서 둥근 월 이동 캡슐, 월 경계를 넘는 어제/오늘, 방문 범위 선택·해제 동기화, 여러 달 선택 순서·미리보기·요약 보존을 구현. 8월 95장/7월 9장 실기기에서 선택 후 월 왕복 유지 확인 |
| 2026-08-10 | 전체 화면 설정 패널 | 디자인 | 전체 화면 Dialog의 투명 시스템 바가 뒤 편집 화면의 딤 색을 비춰 iOS 라이트 패널과 달리 회색 상태 표시줄로 보임 | 빌드 470에서 사진첩·Quick·자막·음악·엔딩·사진시간·영상구간·클립 미리보기 Dialog의 딤을 제거하고 배경색·명암 아이콘을 패널에 맞춰 통일, 실기기 캡처 확인 |
| 2026-08-10 | 삼성 HEIC 위치·엔딩 | 데이터·권한·합성 | MediaStore가 Android 10 이상에서 원본 GPS를 제거해 삼성 HEIC의 위치가 비고, 엔딩 적용 뒤 자막용 안내 문구가 표시됨 | 빌드 471에서 `ACCESS_MEDIA_LOCATION` 요청, `MediaStore.setRequireOriginal`과 AndroidX ExifInterface HEIC 읽기를 적용하고 엔딩 전용 안내 문구로 분리. SM-F968N의 2026-07-31 HEIC에서 `고양시`·`7. 31.`을 5종 미리보기 모두 확인하고 오피스 엔딩 18.5초·1440×1080 MP4를 HanClip 앨범에 실제 저장 |
| 2026-08-10 | 카피라이터 설명·내장 서체 | 텍스트·아이콘·디자인·합성 | Android 정보 화면은 iOS의 세부 기능 설명과 항목별 의미 아이콘이 일부 없고, 엔딩 프리셋이 참조하는 Paperlogy·NEXON Lv.1 Gothic·Poppins 파일도 없어 Pretendard로 조용히 대체됨 | 빌드 472에서 고정 iOS 정보 항목 46/46개와 Quick·여행·Ai·AiShot·컬렉션·묶음사진·자클립·브라우저·엔딩 설명, 의미별 아이콘을 복원. 16종 서체명·파일크기·실제 글꼴 샘플 표를 추가하고 iOS 원본 서체 3개와 전체 `font-licenses`를 빌드 시 공유 자산으로 생성해 자막/엔딩 미리보기와 최종 MP4 렌더러에 연결. 빌드 473에서 자막 전체 서체 목록도 실제 글꼴로 표시하고 글자·그림자색 대비가 높은 iOS 8색 파스텔 미리보기 배경을 적용 |
| 2026-08-10 | 컬렉션 압정 | 디자인·공유 자산 | Android는 고정 상태에 일반 Material 압정 아이콘을 사용해 iOS가 새로 추가한 실제 빨간 압정 이미지와 달랐음 | 빌드 473에서 고정 iOS의 `CollectionPin.png`를 공유 생성 자산으로 패키징하고 포스터 상단 고정 상태에 동일 원본 이미지를 표시하며, 자산 로드 실패 때만 기존 아이콘으로 안전하게 대체 |
| 2026-08-10 | 엔딩 실제 글꼴 | 합성·디자인 | 자막 설정과 미리보기는 선택 글꼴을 표시하지만 최종 엔딩 JPEG/MP4는 기본 글꼴을 사용하고, 보물지도·랜드마크도 지정 글꼴을 합성에 반영하지 않음 | 빌드 474에서 가져온 글꼴을 포함한 공통 글꼴 로더를 워터마크와 엔딩 렌더러가 공유하도록 통합하고, 자막형은 선택 글꼴·그림자, 보물지도는 고운바탕, 랜드마크는 마루부리를 최종 합성에 적용. ReleaseQA 컴파일·패키징과 SM-F968N 데이터 보존 설치 확인 |
| 2026-08-10 | 일정·랜드마크 배치 | 합성·디자인·텍스트 | Android 최종 합성은 여행일정을 세로 표로 그리고 랜드마크를 최대 4개 카드로 제한해, iOS의 날짜 배지 지그재그 일정과 전체 방문지 경로·지역별 명소 범위가 축소됨 | 빌드 475에서 화면비에 따라 3/5열로 바뀌는 지그재그 일정 경로, 날짜 캡슐·국가 이동 표식, 3/4열 전체 랜드마크 경로를 이식. iOS의 국내외 지역 명소 사전 67개를 Android에도 동일하게 확장하고 ReleaseQA 빌드·SM-F968N 설치 확인 |
| 2026-08-10 | 자막형 엔딩 배치 | 합성·디자인 | Android는 방문지를 날짜가 붙은 세로 타임라인으로 표시해 iOS의 큰 촬영기간과 도시·차량·항공이 이어지는 중앙 정렬 흐름형 배치와 달랐음 | 빌드 476에서 왼쪽 여행 기록 제목, 큰 촬영기간, 선택 글꼴로 폭·높이에 맞춰 자동 축소/줄바꿈되는 중앙 흐름형 방문 경로와 국가별 이동 표식을 최종 합성에 이식. ReleaseQA 빌드·SM-F968N 데이터 보존 설치 확인 |
| 2026-08-10 | 엔딩 촬영기간 범위 | 메타데이터·합성 | Android는 위치가 있는 미디어만 최종 엔딩 렌더러에 전달해 위치 없는 앞뒤 클립이 촬영기간에서 빠지고, 위치는 있으나 날짜가 없는 항목은 일정 날짜가 비었음 | 빌드 477에서 iOS처럼 전체 미디어의 촬영기간을 사용하면서 방문 경로만 위치 있는 항목으로 구성하고, 항목 날짜가 없으면 전체 촬영 시작일을 사용하도록 수정 |
| 2026-08-10 | 엔딩 방문지 병합 | 메타데이터·합성 | Android는 같은 날 가까운 연속 촬영도 주소명이 다르면 별도 지점으로 만들고 해외 방문지마다 국가명을 반복할 수 있어 iOS 경로보다 복잡해짐 | 빌드 478에서 iOS와 동일하게 같은 날 5km 이내 연속 위치를 합치고, 대한민국은 도시만·해외는 국가가 처음 나오거나 바뀔 때만 국가명과 도시를 함께 표시하도록 통일 |
| 2026-08-10 | 엔딩 패널·자막 배경 | 합성·디자인 | Android 패널은 화면 높이 비율 여백이라 세로 영상에서 iOS보다 작아지고 테두리가 없었으며, 자막형 배경은 균일한 검정막으로 그림자색 그라데이션이 빠짐 | 빌드 479에서 iOS의 짧은 변 기준 30/37.5pt 여백·26pt 모서리·1.2pt 테두리를 같은 비율로 이식하고, 배경 사진 46%·검정 56%→그림자색 48% 그라데이션·패널 38% 투명도를 적용 |
| 2026-08-10 | 오피스 보고서 구조 | 합성·디자인·텍스트 | Android 오피스 엔딩에는 iOS의 방문지역 요약·작성자·NO./DATE/REGION/MOVE 표 머리글과 번호 열이 없고, 모든 테마 하단에 iOS에 없는 HANCLIP 꼬리표가 표시됨 | 빌드 480에서 문서 머리글·기간·요약 바·4열 표·교대 행·이동수단을 동일 구조로 복원하고, iOS 렌더러에 없는 공통 하단 꼬리표를 제거 |
| 2026-08-10 | 엔딩 설정 미리보기 재동기화 | 디자인·텍스트·서체 | 최종 합성 교정 뒤에도 설정 미리보기는 영문 임시 제목·세로 일정 표·최대 3개 랜드마크 카드·공통 HANCLIP 꼬리표를 유지하고 선택 글꼴을 반영하지 않음 | 빌드 481에서 테마별 실제 한글 제목, 선택/지정 글꼴, 중앙 흐름형 자막, 날짜 캡슐 일정, 전체 경로형 랜드마크, 오피스 4열 머리글로 맞추고 공통 꼬리표 제거 |
| 2026-08-10 | 달력 썸네일 롱터치 | 버튼·기능·디자인 | iOS는 선택 썸네일을 길게 눌러 크게 보고 해당 항목을 선택에서 제외하지만 Android 썸네일에는 롱터치 동작이 없음 | 빌드 482에서 롱터치·사진 미리보기·선택 제외를 추가하고, 빌드 483에서 70% 폭 둥근 패널과 영상 자동·반복 재생 및 컨트롤을 동기화. 실기기 시험에서 최근 선택 줄에만 연결된 범위 오류를 발견해 빌드 484에서 달력 본문 선택 썸네일에도 직접 연결 |
| 2026-08-10 | 브라우저 상단 롱터치 | 버튼·기능 | iOS는 뒤로 버튼을 짧게 누르면 이전 페이지·길게 누르면 닫기, 주소 실행 버튼을 길게 누르면 클립보드 주소를 붙여 바로 이동하지만 Android는 닫기와 주소 이동의 짧은 탭만 있음 | 빌드 485에서 뒤로/닫기 상태 전환과 롱터치 닫기, 주소 실행/로딩 중지 상태 전환과 롱터치 클립보드 이동, 햅틱·접근성 라벨을 동기화 |
| 2026-08-10 | 테마 선택 순서 | 기능·상태·버튼 | iOS는 사용자 테마 3개의 색상 견본을 길게 끌어 순서를 바꾸고 로고 짧은 탭의 순환 순서에도 반영하지만 Android는 고정 순서만 제공 | 빌드 486에서 기본 3개는 고정하고 사용자 테마 3개만 색상 견본 롱드래그로 이동, 영속 저장·복원과 로고 순환 순서 반영 및 접근성 라벨을 동기화 |
| 2026-08-10 | 개봉 앨범명 | 기능·텍스트·저장 | iOS는 사진 앱 개봉 전에 앨범명을 직접 입력하지만 Android는 `HanClip` 고정 폴더만 표시·사용 | 빌드 487에서 80자 앨범명 입력, 빈 값 기본 복원, 파일 경로 금지문자 안전 치환과 Android 8~최신 MediaStore의 실제 사용자 앨범 저장을 동기화 |
| 2026-08-10 | 테마 패널 표기 | 텍스트·디자인 | Android는 iOS의 `SELECT THEME`, 영문 테마명, `COLOR SYSTEM`과 `Main/Sub/BG/Text`를 한국어로 번역하고 Automatic 행에도 iOS에 없는 견본을 표시 | 빌드 488에서 고정 iOS 원문 표기와 Automatic 견본 생략을 동일하게 적용 |
| 2026-08-10 | 자막 프리셋 사용자값 | 기능·상태·롱터치 | iOS는 프리셋을 고른 뒤 바꾼 글자색·그림자색·그림자 진하기·크기·줄 간격을 프리셋별로 기억하고 초기화 버튼을 길게 눌러 전체 기본값으로 되돌리지만 Android는 고정값만 다시 적용 | 빌드 489에서 15개 프리셋별 사용자 외형 저장·재적용, 서체가 바뀌면 프리셋 추적 중단, 초기화 버튼 롱터치 전체 삭제와 안내를 동기화 |
| 2026-08-10 | 넓은 화면 홈 | 적응형 디자인 | 바형 기준 3열 프리셋·2열 컬렉션을 큰 화면에서도 그대로 늘리면 태블릿·트라이폴드·Fold 펼침 폭을 낭비함 | 빌드 490에서 activity 리사이즈를 명시하고 600dp 이상 프리셋 6열·컬렉션 3열, 1200dp 이상 컬렉션 4열로 재배치하며 기존 상태 보존형 Compose 구조를 유지 |
| 2026-08-10 | 개봉 화면 아래로 닫기 | 제스처·디자인 | iOS 개봉 저장 화면은 아래로 밀어 닫을 수 있지만 Android는 닫기 버튼과 시스템 뒤로만 종료됨 | 빌드 491에서 화면을 아래로 끄는 동안 패널이 손가락을 따라 이동하고 96dp 이상이면 닫히며, 미달·취소 시 원위치하도록 동기화 |
| 2026-08-10 | 넓은 화면 미디어 선택 | 적응형 디자인·작업 효율 | 사진·영상 썸네일이 큰 화면에서도 5열로 커져 한 화면에서 비교할 수 있는 미디어 수가 늘지 않음 | 빌드 492에서 바형은 5열을 유지하고 600/840/1200dp에서 8/10/12열로 전환해 펼침 화면에서 더 많은 원본을 동시에 비교·선택하도록 개선 |
| 2026-08-10 | 넓은 화면 편집 | 적응형 디자인·작업 효율 | 편집 화면이 큰 화면에서도 긴 단일 열이라 클립을 비교하며 순서·시간을 조정하기 어려움 | 빌드 493에서 헤더·전체 설정은 전체 폭을 유지하고 600dp 이상 클립만 2열로 배치해 비교·수정 밀도를 높이며, 바형은 기존 1열과 상태를 그대로 유지 |

## 전체 동등성에서 남은 외부 결정·플랫폼 작업

| 항목 | 현재 Android 상태 | 완료에 필요한 것 |
| --- | --- | --- |
| Google Play 유료 워터마크 | 사용자 요청에 따라 테스트 기간에는 워터마크 끄기를 포함한 모든 기능을 결제 상태와 무관하게 개방하고, 가짜 구매 복원 동작 대신 `테스트 기간 · 전체 기능 사용 중`을 표시 | 추후 사용자가 결제 작업을 요청하면 Play Console 상품 ID를 확정한 뒤 BillingClient 구매·복원·권한 저장 및 실제 결제 시험 |
| 잠금 화면 위젯 호스트 | 사진·퀵모드·AiShot 표준 App Widget을 `home_screen|keyguard`로 제공 | 실제 잠금 화면에 일반 App Widget을 허용하는지는 제조사·런처 정책에 따르므로 SM-F968N One UI의 위젯 호스트에서 최종 확인 |

## 기능·동작·데이터 구조 (35항목, 70점)

| # | 항목 | 상태 | 점수 | 남은 핵심 차이 |
| ---: | --- | --- | ---: | --- |
| 1 | 홈 프리셋 진입 | 완료 | 2 |  |
| 2 | 테마 선택·저장 | 완료 | 2 |  |
| 3 | 화면 꺼짐 방지 | 완료 | 2 |  |
| 4 | 편집 가능한 다중 프로젝트 | 완료 | 2 | 총 10개·AiShot 2개 제한 동기화 |
| 5 | 프로젝트 핀·메모·정렬 | 완료 | 2 | 핀 5개·핀 우선/최신순 동기화 |
| 6 | 프로젝트 미디어·썸네일 영속 저장 | 완료 | 2 | 프로젝트별 원본·JSON·최대 480px JPEG 썸네일 영속 저장 |
| 7 | 사진첩 다중 가져오기 | 완료 | 2 |  |
| 8 | 날짜별 달력 가져오기 | 완료 | 2 |  |
| 9 | 파일 가져오기 | 완료 | 2 |  |
| 10 | 공유 인박스 가져오기 | 완료 | 2 |  |
| 11 | Live Photo 정지·모션 전환 | 완료 | 2 | XMP 무복사 분류·전체 파일 MP4 스트리밍 탐색/추출·원본/정지본 저장·사진/Live 전환/내보내기 구현; 2026-08-09 SM-F968N 실제 Samsung Motion Photo에서 Live 필터·가져오기·사진 2.0초→모션 영상 3.3초 전환·H.264 MP4 렌더링·시사회 재생 확인 |
| 12 | 기본시간 0.1~30초·0.1초 증감 | 완료 | 2 | 2026-08-09 동기화 |
| 13 | 영상 구간 미리보기·트림 | 완료 | 2 |  |
| 14 | 클립 순서 변경 | 완료 | 2 |  |
| 15 | 삭제·초기화·되돌리기 | 완료 | 2 |  |
| 16 | 비슷한 사진 자동 묶음 | 완료 | 2 | 16×16·얼굴 수·3모드·간격·시간/비율/거리 수치 동기화 |
| 17 | 영상 타격점 다중 분할 | 완료 | 2 |  |
| 18 | 오디오 타격 분류 수치 | 완료 | 2 | iOS와 동일한 192 버킷·분류/말소리 억제 수치·시간 버킷 내림; 12.131초 실영상에서 192개 파형·다중 타격점 생성 확인 |
| 19 | 자동·1:1·3:4·4:3·9:16·16:9 | 완료 | 2 | 2026-08-09 누락 비율 복구 |
| 20 | 출력 해상도 산정 | 완료 | 2 |  |
| 21 | 자막 텍스트·위치·색상·그림자 | 완료 | 2 |  |
| 22 | 저작권 로고·플랫폼·주소 | 완료 | 2 | iOS 원본 플랫폼 PNG·사용자 이미지 가져오기·미리보기·MP4 비트맵 합성 |
| 23 | 내장·가져온 글꼴 | 완료 | 2 | TTF/OTF 가져오기·30개 제한·미리보기·내보내기 적용 |
| 24 | 배경음악 파일·샘플 선택 | 완료 | 2 | 2026-08-09 iOS 6개 샘플 동기화 |
| 25 | 음악·원본 볼륨 | 완료 | 2 |  |
| 26 | 음악 반복·페이드 인·아웃 | 완료 | 2 | iOS와 같은 0.3초/1.0초 램프 |
| 27 | 온라인 음악 브라우저·즐겨찾기 | 완료 | 2 |  |
| 28 | MP4 구성·내보내기 | 완료 | 2 | 플랫폼 코덱 차이 제외 |
| 29 | 시사회·전체화면 반복 재생 | 완료 | 2 |  |
| 30 | 갤러리·파일 저장·공유 | 완료 | 2 |  |
| 31 | AiShot 수동 촬영 | 완료 | 2 |  |
| 32 | AiShot 소리 자동 감지 | 완료 | 2 |  |
| 33 | AiShot 화면 변화 보조 판정 | 완료 | 2 | 8×8·0.22초·0.75초 신호와 iOS 동일 가중치/문턱값, 실기기 활성 확인 |
| 34 | AiShot 앞·뒤 버퍼와 샷 길이 | 완료 | 2 | iOS처럼 연속 임시 녹화 후 타격 시각 기준 절단·자동 재시작; 일반 2+3초를 실기기 연속 세션에서 5.022/5.023/5.013초로 확인하고 3개 URI 일괄 편집 전달 검증 |
| 35 | 빠른 실행 URL | 완료 | 2 |  |

기능 점수: **기존 70/70 표기는 무효, 세부 동작 재검증 중**

## 디자인 (15항목, 30점)

주요 Android 화면은 iOS 소스의 수치·색상과 제공된 기준 스크린샷을 나란히 비교해
간격·크기·상태를 검증했다. OS 고유 상태 표시줄과 폰트 렌더링 차이는 동일한
제품 화면을 만족하면 동등한 것으로 본다.

| # | 화면 | 상태 | 점수 |
| ---: | --- | --- | ---: |
| 36 | 홈 상단 헤더 | 완료 | 2 |
| 37 | 홈 프리셋 그리드 | 완료 | 2 |
| 38 | 홈 저장 프로젝트 영역 | 완료 | 2 |
| 39 | 편집 상단 헤더·미리보기 | 완료 | 2 |
| 40 | 편집 요약·전역 설정 | 완료 | 2 |
| 41 | 클립 행 | 완료 | 2 |
| 42 | 순서 변경 영역 | 완료 | 2 |
| 43 | 하단 만들기 영역 | 완료 | 2 |
| 44 | 자막 설정 | 완료 | 2 |
| 45 | 음악 설정 | 완료 | 2 |
| 46 | 영상·사진 시간 설정 | 완료 | 2 |
| 47 | 달력·미디어 선택 | 완료 | 2 |
| 48 | 시사회·저장 위치 | 완료 | 2 |
| 49 | AiShot 카메라 | 완료 | 2 |
| 50 | 온라인 음악 브라우저 | 완료 | 2 |

디자인 점수: **기존 30/30 표기는 무효, 화면별 재검증 중**

## 현재 유효 합계

**미산정 — 기존 100/100 평가는 철회**

기존 평가는 화면이나 기능의 큰 구조가 존재하면 완료로 처리해 롱터치, 관리 버튼,
저작권 원문, 개별 아이콘 같은 세부 항목의 누락을 반영하지 못했다. 텍스트·아이콘·버튼
동작·페이지·디자인을 실제 기기에서 다시 확인한 항목만 점수에 포함하고, 전체 재검증이
끝난 뒤 새 점수를 산정한다.

이 수치는 구조적 대응률이며 Apple·Android의 플랫폼 고유 사진 선택기, 공유 UI, 코덱
출력 차이는 동일한 제품 동작을 충족하면 동등한 것으로 본다. 디자인 2점은 동일 기기
폭의 기준 스크린샷을 나란히 비교해 간격·크기·색상·상태를 확인한 뒤에만 부여한다.

## 2026-08-09 대규모 변경 이식 진행률

이 표의 백분율은 사용자와 합의한 다섯 작업 묶음 안에서의 구현·검증 진척도다. 기존
50항목 동등성 점수와는 별개이며, 실기기와 저장 데이터까지 확인한 결과만 반영한다.

| 작업 묶음 | 현재 진척 | 이번 범위에서 확인한 내용 | 남은 핵심 작업 |
| --- | ---: | --- | --- |
| 컬렉션 저장소·기존 데이터 보존·Quick/Life 기반 구조 | 100% | 내부 원본/포스터/JSON, fsync·백업 인덱스 복구·동시 쓰기 잠금·인덱스 선반영 후 파일 삭제, 기존 완성본 무손실 이관, 6종 프리셋, Quick/Life 기본값, 새 MP4 자동 등록. 빌드 451에서 최신 Quick 전체 화면 구조와 홈 빠른 실행 3종 반영 | 이 작업 묶음 완료. iOS 세부 카드 수치는 디자인 묶음에서 계속 비교 |
| 통합 미디어 선택·중복 방지·취소 가능한 가져오기 | 100% | 공통 URI 계약, 기존 원본 URI 중복 제외, 컬렉션 SHA-256 중복 방지, 64KB 단위 취소를 공유. 원본 URI 선선택·안전한 해제 동기화·빈 선택 적용·되돌리기에 더해 빌드 470에서 월 이동과 여러 달 선택 순서·미리보기·가져오기 요약 보존을 구현하고 8월 95장/7월 9장 실기기 왕복 확인 | 플랫폼 자산 식별자 형식은 다르지만 동일 제품 동작을 충족해 이 작업 묶음 완료 |
| 위치·촬영기간·MP4 메타데이터 처리 | 100% | 사진 EXIF·영상 GPS 읽기와 역지오코딩, 원본 위치의 프로젝트 영속 보존, 대표 위치 선정, MP4 표준 위치와 Android/iOS `HANCLIP_METADATA` 기록·복원. 빌드 469 실제 MP4에서 JSON 배열·무효 위치 제거와 iOS Swift Codable 디코딩을 확인하고, 빌드 471에서 원본 위치 권한이 필요한 삼성 HEIC도 `고양시`·`7. 31.`로 실기기 확인 | iPhone 사진 앱으로 옮겨 컬렉션 표시를 사람이 직접 보는 최종 확인은 권장 회귀 시험으로 유지 |
| 5종 엔딩 카드와 영상 합성 | 100% | 5개 테마 선택·미리보기·1~10초(0.5초 단위) 저장과 마지막 클립 합성. 빌드 470에서 보물지도 곡선 variation, 일정표 헤더, 지역별 랜드마크, 오피스 문서 구조까지 실제 렌더러를 동기화하고, 빌드 471에서 위치 포함 삼성 HEIC로 5종 미리보기와 오피스·보물지도 엔딩 MP4 저장 확인. 빌드 474에서 자막형 선택 글꼴·그림자와 보물지도·랜드마크 지정 글꼴을 실제 최종 합성 경로에 연결 | 테마별 별도 MP4 프레임 단위 비교는 기능 누락이 아닌 권장 시각 회귀 시험으로 유지 |
| iOS 디자인 일치·실기기 테스트·성능 및 회귀 수정 | 99% | 6종 홈 프리셋 3열×2행, 컬렉션 포스터/핀, 접히는 프리셋 설정, Quick과 독립 엔딩·자막·음악 전체 화면을 반영. 빌드 470에서 미디어 선택 월 탐색과 전체 화면 Dialog 시스템 바까지 공통 정리하고 홈·선택기를 실기기 캡처로 확인. 빌드 472에서 카피라이터의 전체 도움말·의미별 아이콘과 누락 서체의 실제 미리보기/합성을 고정 iOS 기준으로 복원 | OS 고유 상태 표시줄·글꼴 래스터 차이를 제외한 나머지 화면의 픽셀 단위 사람 검수 |

사용자와 합의한 범위 가중치(7, 4.5, 3.5, 6, 3.5)를 적용한 현재 가중 평균은 **약 99.9%**다.

## 2026-08-10 iOS `c18c3a8e` 고정 기준 동기화

- 빌드 495에서 홈 프리셋의 `Quick`을 iOS와 같은 `퀵모드`로 바꾸고 설명을 `고르면 바로 영화로`, 아이콘을 마법봉이 아닌 번개로 맞췄다. 인생 영화 설명도 `삶의 순간을 한 편으로`로 동기화했다.
- 컬렉션 최대 보관 수를 20개에서 30개로 늘렸다. 새로 가져오는 영상은 기기 안에서 분산 장면의 밝기·대비·선명도를 비교해 포스터를 고른다.
- 포스터 롱터치 메뉴에 `썸네일 AI 재선택`을 추가했다. 디바이스 AI 8개와 한클립 AI 8개를 좌우로 비교하고, 실제 포스터의 제목·위치·재생시간을 적용한 채 고르거나 다른 후보를 재생성한다. 840dp 이상에서는 각 AI 후보를 두 열씩 표시한다.
- 후보 이미지는 화면 표시용으로 절반 크기·RGB_565로 디코딩해 iOS와 같은 비교 기능을 유지하면서 Android 메모리 사용량을 낮췄다.
- 사용자가 작업 중 추가한 최신 iOS 변경을 다시 읽어 컬렉션 포스터와 AI 후보에 MP4 파일 크기(MB/GB)를 함께 표시하고, 후보 번호와 핀 상태도 실제 포스터와 같게 반영했다.
- 이 시점에 남았던 핀 포스터 드래그 순서 변경, 기존 포스터 최초 1회 자동 재선정, 기기 방향 직접 감지 컬렉션 플레이어는 아래 빌드 497에서 구현·검증했다.

## 2026-08-10 최신 작업 트리 심층 동기화 · Android 빌드 496

- iOS에 새로 추가된 컬렉션 `파일 용량 줄이기`를 1080p/720p/540p 선택, 예상 용량, 진행률, 취소, 더 작아졌을 때만 안전 교체하는 흐름으로 이식했다. 기존 JSON과 포스터·제작/촬영/위치 메타데이터는 그대로 유지한다.
- 컬렉션 포스터의 정보 순서를 위치→제작→촬영기간→파일 크기→재생시간으로 맞췄고 제작일과 촬영일이 같은 경우 중복 촬영기간을 숨긴다. 전체화면 플레이어는 3초 컨트롤 타임아웃과 아래로 밀어 닫기를 추가했다.
- 공용 사진 화면에 iOS와 같은 촬영일/추가일 오름·내림 정렬, 1/3/5/10분 및 직접 입력 영상 길이 필터, 필터 해제 시 이전 미디어 종류 복원, 한국 공휴일 이름/색상 표시를 추가했다. 카피라이터 도움말 제목은 iOS와 Android 모두 48개로 일치한다.
- iOS 모델 대조에서 Android에 없던 `isVideoSegmentSelected`를 영속 스키마에 추가했다. 이제 자클립 삭제는 원본 행을 없애지 않고 완성본에서 제외하며, 같은 행의 `사용`으로 되돌릴 수 있다. 이전 프로젝트는 기본 `true`로 읽어 데이터 호환을 유지한다.
- AiShot 수동 촬영 버튼에 iOS 원본 6×6 골프 스윙 스프라이트를 적용했다. Paperlogy·NEXON Lv.1 Gothic·Poppins와 전체 글꼴 라이선스는 Gradle의 기존 iOS→Android 공유 자산 생성 경로로 패키징되며, 생성본과 원본의 SHA-256 동일성을 다시 확인했다.
- 이 시점에 남았던 편집 종료 세 선택, 브라우저 영상 감지, 컬렉션 핀 드래그·포스터 자동 보정, 빠른 실행 위젯은 아래 빌드 497에서 구현·검증했다. Google Play 실제 결제만 외부 상품 등록이 필요하다.

## 2026-08-10 잔여 동작 심층 동기화 · Android 빌드 497

- 편집 종료 팝업을 iOS와 같은 `홈`·`저장`·`저장 후 홈`으로 분리했다. 편집 진입 시 프로젝트 스냅샷을 잡아 `홈`은 이번 세션의 자동 저장 변경을 되돌리고 핀·메모·저장 시각을 보존하며, `저장`은 편집 화면에 남고 이후 취소 기준도 새 저장점으로 갱신한다. 새 프로젝트의 `홈`은 자동 생성된 프로젝트까지 정리한다.
- 컬렉션 JSON에 이전 데이터와 호환되는 핀 시각·포스터 선택 버전을 추가했다. 고정 포스터는 메뉴에서 앞으로·뒤로 순서를 바꿀 수 있고, 예전 포스터는 홈 스크롤과 분리된 IO 작업에서 한 장씩 AI 재선정하며 iOS와 같은 진행 패널을 표시한다.
- 컬렉션 영화는 일반 시사회 화면을 거치지 않고 전용 전체화면 플레이어로 바로 열린다. 시스템 회전 잠금과 별개로 기기 방향 센서를 따라 영상·조작 버튼을 회전하고 반복·맞춤/채움·3초 컨트롤·아래로 밀어 닫기를 유지한다.
- 내부 브라우저가 HTML video와 직접 영상 링크를 감지해 `영상 / 받기 / 닫기` 패널을 표시한다. 음악·영상 다운로드는 앱 내부에 파일명·결정/불확정 진행률·취소를 표시하고 완료 파일은 `Downloads/HanClip`에 보존한다.
- 사진·퀵모드·AiShot 3종 빠른 실행 App Widget을 추가했다. 둥근 한클립 패널을 홈 화면과 이를 지원하는 잠금 화면 위젯 호스트에 배치할 수 있으며 기존 `hanclip://` 라우팅을 공유한다.
- 고정 포스터의 빨간 핀을 길게 끌면 가로 이동은 한 칸, 세로 이동은 현재 열 수만큼 순서를 바꾼다. 카드 전체 탭·롱터치와 핀 입력 레이어를 분리했으며 SM-F968N 외부 화면에서 핀 탭 저장, 인접 포스터 한 칸 드래그 교환, 임시 핀 8개 원상 복원을 확인했다.
- 아직 외부 상태가 필요한 항목은 Google Play 상품 등록과 실제 결제 시험이다.

## 2026-08-10 테스트 기간 전체 기능 개방 · Android 빌드 498

- 결제 기능은 사용자 요청 전까지 구현하지 않는다. 현재 Android에는 실제 유료 권한 검사나 기능 잠금 분기가 없으며 워터마크 끄기, 로고·자막·음악·엔딩·내보내기를 포함한 모든 기능을 결제 상태와 무관하게 사용할 수 있다.
- 카피라이터 화면의 동작하지 않는 `구매 복원` 버튼을 제거하고 `테스트 기간 · 전체 기능 사용 중` 상태 안내로 교체했다. 추후 결제 도입 시 이 정책 지점에서만 유료 권한을 연결한다.

## 2026-08-10 자막·음악 실화면 세부 교정 · Android 빌드 499

- 빈 자막 미리보기에 iOS 기준의 `여기에 글을 넣으세요 / I Love you ♡ / +82 10-0000-0000` 3줄 예시를 현재 프리셋의 글꼴·색상·그림자로 표시해 스타일을 선택하기 전에도 효과를 비교할 수 있게 했다.
- 음악 샘플 2×3 카드에 선택 체크와 미선택 파형 아이콘을 복원하고, 카드·글자·재생 버튼 밀도를 iOS 실화면에 맞춰 줄여 긴 제목과 설명의 불필요한 잘림을 완화했다.
- 펼친 워터마크 설명에 남아 있던 유료 제거·복원 문구를 테스트 기간 전체 기능 개방 안내로 교체했다.

## 2026-08-10 음악 카드 한 줄 제목 안정화 · Android 빌드 500

- 커버 화면의 2열 샘플 카드에서 긴 제목이 두 줄이 되며 설명을 밀어내던 문제를 수정했다. 선택/파형 아이콘과 재생 버튼을 소폭 축소하고 제목을 한 줄로 고정해 `광고 클래식 드라마`, `지우에게 첫눈이란`도 설명과 함께 카드 안에 안정적으로 배치한다.

## 2026-08-10 넓은 화면 저장 영화 밀도 개선 · Android 빌드 501

- 갤럭시 탭 SM-T500의 800dp 실화면에서 프리셋은 6열인데 일반 영화가 한 줄 전체를 차지하던 공간 낭비를 확인했다. 휴대폰은 iOS 기준의 1열을 유지하고, 600dp 이상 태블릿·폴드 펼침 화면에서는 일반 영화 8칸을 2열로 배치해 같은 화면에서 더 많은 프로젝트를 비교하도록 개선했다.

## 2026-08-10 태블릿 컬렉션 3열 고정 · Android 빌드 502

- 사용자 지정에 따라 휴대폰 컬렉션은 2열을 유지하고, 600dp 이상 태블릿·폴드 펼침 화면은 폭이 더 넓어져도 3열로 고정했다. 초광폭 화면에서 임의로 4열이 되어 포스터가 지나치게 작아지는 분기를 제거했다.

## 2026-08-10 AiShot 촬영 버튼 원형 마스크 · Android 빌드 503

- SM-F968N 실기기에서 중앙 수동 촬영 버튼의 골프 스윙 프레임이 정사각형으로 원형 링 밖을 덮는 차이를 확인했다. 스프라이트 재생과 촬영 동작은 유지하고 이미지에 원형 클립을 적용해 iOS 촬영 버튼과 같은 원형 미리보기 구조로 교정했다.

## 2026-08-10 AiShot 감도 패널 구조 교정 · Android 빌드 504

- iOS 실화면처럼 음량 막대 왼쪽에 상태 점, 오른쪽에 `감지 중`/`저장 중`을 배치하고 다음 줄에 파형 아이콘·`감도`·4개 감도 버튼을 균등 배치했다. 소리 분석과 자동 감지 수치는 변경하지 않았다.

## 2026-08-10 AiShot 감도 버튼 단일행 안정화 · Android 빌드 505

- 커버 화면에서 Material 기본 칩 여백 때문에 `시끄러움`·`조용함`이 두 줄로 꺾이는 실기기 문제를 확인했다. 동일한 둥근 선택 상태를 유지하는 소형 버튼으로 바꿔 네 감도명이 한 줄에 표시되도록 교정했다.
- SM-F968N 120Hz 홈에서 5회 위아래 왕복 후 558프레임, jank 3.05%, p50 5ms, p90 8ms, p95 9ms, p99 15ms, 느린 비트맵 업로드 0건을 확인해 저장 영화 2열 분기 추가 뒤에도 기존 스크롤 최적화가 유지됨을 검증했다.

## 2026-08-10 Android themed launcher icon · Android 빌드 506

- Android 13 이상에서 시스템 테마 색을 적용할 단색 런처 아이콘 정의를 추가했다. 일반·원형 런처 아이콘의 기존 한클립 로고와 배경은 유지하고, 시스템이 같은 로고 알파 마스크를 색칠하도록 구성했다.
- SM-F968N에서 데이터 삭제 없이 process-cold 실행한 결과 221ms에 홈 `영화 프리셋`으로 시작했고, 카메라 자동 진입 없이 기존 프로젝트 10/10을 유지했다.
- `dumpsys appwidget`에서 SM-F968N과 SM-T500 모두 `HanClipQuickWidgetProvider`가 홈·잠금 화면 범주(`widgetCategory=3`)의 정상 provider로 등록되고 zombie/suspended 상태가 아님을 확인했다.

## 2026-08-10 롱터치 접근성·피드백 보강 · Android 빌드 507

- 홈 로고의 테마 선택과 플로팅 i 버튼의 음악 브라우저 열기 롱터치에 진동 피드백을 추가했다.
- TalkBack이 롱터치 결과를 클릭 노드에서 바로 읽도록 홈 로고·i 버튼과 내부 브라우저의 닫기·주소 이동·즐겨찾기 버튼 설명을 실제 `combinedClickable` 접근성 노드에 병합했다.
- SM-F968N·SM-T500에서 앱 아이콘 롱터치 바로가기 `사진`·`퀵모드`·`AiShot`이 모두 활성 등록되고, `hanclip://open`과 `hanclip://search`가 각각 홈과 HanClip 내부 음악 브라우저로 열림을 확인했다.

## 2026-08-10 표준 Photo Picker 클라우드 경로 · Android 빌드 508

- 별도 Google Photos API·OAuth 기능은 iOS와 함께 제외한다. Android는 기존 `PickMultipleVisualMedia`를 유지해, 시스템 Photo Picker에 연결된 Google Photos 같은 적격 클라우드 미디어 공급자를 외부 클라이언트 설정 없이 사용한다.
- Android 4.4~10 지원 기기에서 Play 서비스가 표준 Photo Picker 백포트 모듈을 준비하도록 공식 `ModuleDependencies` manifest 선언을 추가했다. 선택기가 없는 기기는 AndroidX가 기존처럼 `ACTION_OPEN_DOCUMENT`로 안전하게 폴백한다.
- SM-F968N의 표준 선택기를 직접 열어 `com.google.android.photopicker` 실행과 `Google 포토 검색`·클라우드 사진 표시를 확인했다. 사진은 선택하지 않았다.

## 2026-08-10 구형 Android 사진 앱 선택 폴백 · Android 빌드 509

- 에디터의 `파일 선택`을 `다른 앱·파일`로 명확히 바꾸고, 표준 `ACTION_GET_CONTENT` chooser로 삼성 갤러리·Google Photos·파일 선택기 중 설치된 앱을 사용자가 고르게 했다. 특정 Google API, 로그인, OAuth 클라이언트는 추가하지 않았다.
- 선택한 항목은 기존 통합 가져오기가 즉시 앱 내부 작업 원본으로 복사하므로, 외부 앱의 임시 읽기 권한을 프로젝트 재개 후까지 유지할 필요가 없다. 순서·중복 제거·취소 처리는 기존 공통 경로를 그대로 사용한다.
- Android 10 SM-T500 실기기의 HanClip 선택 화면에서 `갤러리`·`내 파일`·`포토`·`Drive`·`OneDrive`가 함께 나오는 것을 확인했다. 미디어는 선택하지 않고 취소해 기기 데이터를 변경하지 않았다.

## 2026-08-10 카드 롱터치 의미 보강 · Android 빌드 510

- AiShot 프로젝트·완성본, 일반 프로젝트·완성본, 컬렉션 영화 카드의 롱터치 결과를 TalkBack 동작 라벨로 명시했다. 삭제 롱터치는 기존처럼 확인 단계를 거친다.
- 자막 설정의 초기화 버튼은 롱터치 시 전체 프리셋을 초기화한다는 접근성 라벨을 추가했다. `영화 추가` 포스터의 길게 누르기가 짧은 탭과 동일한 동작을 중복 실행하던 의미 없는 롱터치는 제거했다.
- ReleaseQA를 데이터 삭제 없이 SM-F968N·SM-T500에 설치했고 process-cold 실행을 각각 258ms·1,091ms에 완료했다.

## 2026-08-10 미디어 가져오기·영화 제작 복구 · Android 빌드 513

- iOS 실화면 13장을 기준으로 미디어 선택, 준비, 영화 제작, 개봉 준비, 시사회, 개봉의 책임을 다시 분리했다. 편집 화면의 중복 선택 안내 두 묶음을 제거하고, 위·아래 `+`에서 여는 불투명 둥근 패널에 `AiShot / 사진 / 달력 / 파일`을 동일 순서와 의미의 아이콘으로 배치했다.
- 가져온 작업 원본을 80개 이후부터 무조건 지우던 로직을 제거했다. 수백 개 선택 시 같은 프로젝트의 앞쪽 클립 URI가 사라져 제작이 실패할 수 있었던 원인을 없앴으며, 향후 정리는 활성·저장 프로젝트가 참조하지 않는 파일만 판별하는 프로젝트 인식 방식으로 구현해야 한다.
- 가져오기 상태에 현재/전체/퍼센트를 추가하고, `준비하고 있습니다` 불투명 패널과 취소 동작을 적용했다. 개봉 준비는 첫 클립 미리보기와 진행 와이프, 퍼센트를 표시하고 완료 시 시사회로 이동한다.
- 길고 중복된 완성본 확인 창을 제거해 하단 만들기 버튼에서 개봉 준비로 바로 이동한다. 클립 설정은 iOS 순서에 맞춰 `영상 길이`를 `기본시간` 앞에 두고 기본 펼침 상태로 바꿨다.
- Android 사용자 표기는 `라이브포토`에서 `모션포토`로 통일했다. 내부 `LivePhotoMode` 타입은 이전 프로젝트 JSON 호환을 위해 유지한다.
- SM-F968N에서 앱 데이터 삭제 없이 3.7초 테스트 MP4를 가져와 2초·1080×1440·30fps MP4로 만들고 시사회까지 두 차례 성공했다. 두 번째는 확인 창 없이 개봉 준비 미리보기와 5% 표시를 직접 캡처했다. SM-T500은 다른 앱 개발용이라는 사용자 지정에 따라 build 511 이후 HanClip 설치·실행·검증에서 제외했다.
- 남은 확인: 수백 개 실제 선택의 장시간 취소·재개와 사진+영상 혼합 대용량 합성은 별도 회귀 시험이 필요하다. 이 항목을 확인하기 전에는 미디어 제작 동등성을 100%로 산정하지 않는다.

## 2026-08-10 사진·달력 재구축 동기화 · Android 빌드 517

- iOS의 새 사진 화면처럼 기본 시스템 갤러리가 아닌 앱 내부 날짜별 격자를 사용하고, 상단을 `취소 / 사진 / N개 추가`, 하단을 `필터 / 전날 / 오늘 / 해제 / 추가`의 5개 플로팅 원형 버튼으로 재구성했다.
- 상단 `사진`을 누르면 `취소 / 달력 / 추가` 달력으로, 달력의 `달력`을 누르면 사진으로 순환한다. 달력 하단은 `전날 / 오늘 / 해제 / 추가` 4개 플로팅 버튼이며 상단의 별도 오늘 버튼은 제거했다.
- 사진을 누른 채 가로·세로로 드래그하면 지나간 항목을 연속 선택하거나 해제한다. SM-F968N에서 한 번의 드래그로 44개 선택, 상단 개수 동기화, 해제를 확인했다.
- 삼성 MediaStore 촬영시각이 초 단위로 반환되는 경우를 정규화해 1970년 날짜 묶음을 제거했고, 사진·달력 전환 뒤에도 날짜별 구분과 선택 상태를 유지한다. Android 사용자 표기는 `모션포토`로 유지한다.
- 하단 버튼은 콘텐츠를 밀어 올리는 고정 행이 아니라 썸네일 위에 떠 있고 화면 밖으로 잘리지 않는 오버레이로 배치했다. SM-F968N 822×1918 화면에서 사진 5개와 달력 4개가 모두 화면 안에 표시됨을 캡처로 확인했다.

## 2026-08-10 최신 컬렉션 변경 동기화 · Android 빌드 518

- iOS 작업 트리의 새 선반 하단 숨김 메뉴를 이식했다. `컬렉션 용량 줄이기`를 펼치면 `720p 일괄 변환`·`540p 일괄 변환`과 해상도 이하 유지 안내가 나오며, 각 영상의 현재 해상도를 먼저 읽어 작은 영상은 건너뛴다.
- 일괄 변환은 영화별 진행률을 전체 진행률로 환산하고 취소 시 이미 완료된 결과를 보존한다. 변환·유지·실패 개수와 원본/결과 총용량을 마지막에 알리며 기존 포스터·제작/촬영/위치 메타데이터는 유지한다.
- 컬렉션 전용 플레이어에 1~4배 핀치 확대, 확대 상태 한 손가락 이동, 이동 범위 제한, 더블 탭 100% 복귀를 추가했다. 확대 중에는 아래로 밀어 닫기를 막고, 일반 크기에서는 기존 방향 감지·반복·맞춤/채움·아래로 닫기를 유지한다.
- 플레이어 조작 버튼은 잠시 뒤 자동으로 숨고 화면 탭으로 다시 표시된다. ReleaseQA 컴파일, SM-F968N 데이터 보존 설치, 컬렉션 숨김 메뉴 펼침과 전용 플레이어 재생 프레임을 확인했다. 실제 두 손가락 제스처 감각은 사용자 실기기 확인이 남았다.

## 2026-08-10 iOS 전체 디자인 명세 기반 홈·기본값 동기화 · Android 빌드 522

- 최신 iOS 호출 소스를 기준으로 디자인 시스템, 화면·상태 지도, 컨트롤 기본값·저장 수명, 9개 화면군 상세 명세를 `reference/ios-current/design-spec`에 고정했다. 추가 스크린샷은 중단하고 구조가 바뀔 때만 증분 촬영한다.
- 홈에서 AiShot 별도 목록과 일반 영화 구분을 없애고 iOS처럼 최대 10개의 통합 `영화 목록`으로 구성했다. 프로젝트가 2개 미만일 때만 합계 2행이 되도록 빈 행을 표시하고, 아래는 최대 30개의 `컬렉션` 선반으로 이어진다.
- 영화 프리셋을 3열 기준 높이 108dp, 카드·아이콘 모서리 8dp와 테마 그라데이션으로 맞췄다. 영화 목록은 video-library, 컬렉션은 books 계열 아이콘으로 교정했다.
- 홈 우측 미디어 추가 버튼은 더 이상 새 영화로 바로 이동하지 않고 `AiShot / 사진 / 달력 / 파일` 앵커 메뉴를 연다. 사진·달력·파일은 새 영화 프로젝트를 준비한 뒤 해당 선택 화면을 즉시 열며 AiShot은 카메라로 이동한다.
- i 플로팅 버튼을 44dp serif `i`, bottom 8dp로 다시 만들고 탭은 전체 카피라이터, 0.55초 롱터치는 내부 음악 브라우저 의미를 유지했다. 테마 패널은 불투명 표면, 화면 폭 92%, 상단 정렬로 맞췄다.
- 프리셋 시작 시 기본시간을 iOS와 같은 새 영화2/퀵1/AiShot4/여행1/인생2/골프4초로 매번 적용한다. 새 영화·퀵·인생·골프 자막은 Poppins, `#FFE45C`, `#642BFF`, 그림자 75%, 더크게 기본으로 맞추고 AiShot만 초록 골프 자막을 유지했다.
- AiShot 감도는 앱 전체 `자동` 기본을 유지하되 촬영 길이는 프로젝트별 `일반`에서 복원한다. 카메라는 매 세션 후면, 줌은 1×로 시작해 이전 세션의 전면·확대 값이 새 촬영에 잘못 전파되지 않게 했다.
- SM-F968N에 데이터 보존 설치해 3×2 프리셋, 통합 영화 목록, 둥근 미디어 메뉴, 불투명 테마 패널, 카피라이터 최하단 내장서체 라이선스까지 확인했다. SM-T500과 에뮬레이터는 사용하지 않았다.

## 2026-08-12 고정 기술서 감사 후 안전성·적응형 동기화

- iOS 기준은 `31e60ec5feb100b3dfcec78d47040e54e2d682ca`, Android 시작 기준은 `2b178c337275298b34ac1b3073fb130ba9fab423`으로 고정했다. 과거 완료 표기는 증거에서 제외하고 코드와 새 시험만 사용했다.
- 프로젝트 JSON에 schema version, 생성 시각, 비슷한 사진 대표 간격을 저장한다. 구형 데이터는 v1·영상 다중 구간 기본으로 읽고 값 범위를 보정하며, 손상된 클립 하나가 프로젝트 전체를 폐기하지 않게 격리한다. 메타데이터는 같은 디렉터리 임시 파일을 fsync·decode 검증한 뒤 교체하고 이전 정상본을 backup으로 유지한다.
- 컬렉션 index도 임시 파일 decode 검증과 정상 backup 유지를 적용했다. 손상된 1차 index가 정상 backup을 덮지 않으며, 개별 손상 record는 나머지 영화를 막지 않는다. 압축 결과는 크기뿐 아니라 영상 트랙과 원본 대비 재생 시간 허용오차를 확인한 뒤 교체한다.
- 미디어 가져오기는 작업 원본과 모션포토 추출을 임시 파일→fsync→rename 순서로 확정한다. 취소 시 해당 배치가 아직 편집기에 반영하지 않은 `working-media` 내부 파일만 삭제하고 프로젝트 저장 파일은 경계 밖으로 보존한다.
- AiShot은 탐지 순간의 monotonic sequence로 결과 순서를 고정하고 Preview/VideoCapture target rotation을 기기 방향에 맞춘다. 공통 전체화면 플레이어는 제목·공유·PlayerView scrub, 좌우 double tap ±10초, pinch/pan, 아래 swipe close, phone 센서 방향과 종료 시 정책 복원을 공유한다.
- 홈·편집 핵심 폭 920dp, 테마 패널 620dp, 홈 프리셋 3열을 적용했다. Automatic 테마는 시스템 다크 모드를 따르고, 미디어 격자는 1/3/5/8열 pinch와 가장자리 drag autoscroll을 지원하며 열 상태를 회전 중 보존한다. 갤러리·카메라 권한 거부 화면에는 앱 설정 직접 진입을 제공한다.
- JVM 단위 시험 4개와 `SM-F968N`의 격리 계측 시험 5개로 primary/backup 선택, AiShot 순서, grid step, duration tolerance, 구형 project fixture migration, 재시작 보존, import rollback 경계, project/collection backup 복구를 확인했다. 기기가 잠겨 있어 화면 제스처·회전의 사람 시각 검수는 미검증으로 남기며 `SM-T500`에는 명령을 보내지 않았다.

## 2026-08-12 제작 진행·취소와 클립 미리보기 보강

- 제작 작업마다 단조 증가 token을 발급하고 취소 시 즉시 무효화한다. 취소 전에 등록된 진행·성공·실패 callback은 더 이상 컬렉션 추가, 시사회 이동, 실패 alert로 취소 상태를 덮지 않는다. 취소 중에는 임시 결과 정리 상태를 표시하고 완료 뒤 클립과 설정을 보존했다는 안내로 닫는다.
- 제작 진행 패널은 선택한 출력 화면비를 반영한 최대 260dp 미리보기, 좌→우 진행 mask, 퍼센트, 경과시간과 현재 속도 기준 예상 잔여시간을 `progressMessage` 하나에서 표시한다. 호환 30fps 재시도는 새 시도 기준으로 시간·진행률을 다시 계산한다.
- 클립 미리보기는 기존 autoplay·loop·play/pause·scrub를 유지하면서 현재 위치와 `처음`·`이전`·`다음` 이동을 추가했다. `편집`은 기존 영상 trim/사진 길이 화면으로 연결하고, `삭제`는 원본을 지우지 않는다는 확인 뒤 기존 undo 가능 제외 경로만 사용한다. waveform/peak의 미리보기 내 직접 편집은 남아 있어 전체 항목은 계속 `부분`으로 분류한다.
- JVM 시험으로 경과/예상시간 계산, 시간 범위 보정, 취소 token의 늦은 callback 차단을 확인했다. debug compile·단위 시험·APK·lint는 통과했다. `SM-F968N`은 모델을 읽어 대상 기기임을 확인했으나 `deviceLocked=1`이어서 설치·조작·사람 시각 검수는 수행하지 않았고 `SM-T500`에는 명령을 보내지 않았다.
- 장시간 제작 foreground service+notification은 manifest·프로세스 수명·알림 권한에 영향을 주는 별도 작업이므로 이번 최소 변경에 포함하지 않았다. 따라서 제작 진행 UI 항목도 전체 기준에서는 `부분`을 유지한다.

## 2026-08-12 장시간 제작·미리보기 재생·컬렉션 경계 검증

- 장시간 제작은 `dataSync` foreground service와 저우선순위 진행 알림을 사용한다. Android 13 이상은 첫 제작에서 알림 권한을 요청하되 거부해도 제작은 계속되며, 알림의 `제작 취소`는 UI와 같은 export token만 취소한다. Android 15 timeout도 같은 취소 경로로 임시 결과를 정리한다.
- service 생성 직후 취소되는 경계에서 `startForeground()` 전에 `stopService()`가 실행되면 프로세스가 종료되는 실기기 결함을 계측 시험으로 발견했다. stop 요청을 foreground 승격 뒤까지 보류하는 handshake로 수정했고, 잘못된 token은 현재 제작을 취소하거나 종료하지 않는다.
- 클립 미리보기는 `한 번`·`반복`·`자동 다음`을 명시적으로 선택한다. 자동 다음은 마지막 클립에서 정지하고, 영상의 주요 타격점/후보 수를 표시하며 `편집`에서 기존 waveform·peak trim 화면을 사용한다.
- 컬렉션 선반의 0/1/29/30개와 열 채움 spacer를 순수 정책으로 고정했다. 29개까지 `영화 추가`가 정확히 하나이고 30개에서는 숨기며, 기존 영화는 열 수가 달라도 버리지 않는다. 240개 staged import rollback 계측은 `working-media`만 전부 정리하고 저장 프로젝트 source 8개를 유지했다.
- `SM-F968N`에 데이터 보존 `install -r` 후 foreground service와 기존 persistence를 포함한 계측 시험 7개가 통과했다. 첫 시험은 승격/중지 경쟁으로 process crash를 재현했고 수정 뒤 전체 재실행이 통과했다. 앱 cold launch는 594ms였고 service 잔류가 없으며, cover 822×1918 홈에서 기존 3×2 프리셋과 저장 영화가 유지됨을 확인했다. `SM-T500`에는 명령을 보내지 않았다.
- 제작 진행과 클립 미리보기는 코드·자동 시험·실기기 service 근거가 생겨 `구현됨-미검증`으로 올린다. 실제 장시간 혼합 export의 알림 진행·화면 전환과 사람 제스처 감각은 별도 AT가 남는다.

## 2026-08-12 편집 실수 방지·사용자 자산 원자 저장

- 일반 클립 목록과 순서 변경 화면의 삭제는 더 이상 즉시 실행되지 않는다. 일반 클립, 원본 영상+자동 컷, 자클립 하나, 비슷한 사진 대표의 실제 제외 범위를 각각 설명한 뒤 기존 undo 가능한 `removeClip`만 호출한다.
- 고정 기본값 표에 맞춰 새 프로젝트의 `클립 설정`은 접힌 상태로 시작하고, 클립 미리보기는 자동 진행·반복이 모두 꺼진 `한 번` 상태로 시작한다. 알림 권한 요청 중 구성 변경에도 제작 대기 상태를 `rememberSaveable`로 유지한다.
- 사용자 글꼴 31번째 가져오기가 가장 오래된 글꼴을 조용히 삭제하던 동작을 제거했다. 30개를 그대로 보존하고 새 파일만 거절하며, 임시 파일 fsync→SFNT/OTF/TTC header·table 범위→Typeface 검증→rename으로 확정한다. Android Typeface가 임의 텍스트도 받아들이는 실기기 동작을 시험에서 발견해 구조 검증을 추가했다.
- 사용자 copyright 로고와 외부 배경 음악도 임시 파일 fsync·decode/audio-track 검증 뒤 원자 교체한다. 잘못된 로고는 이전 정상 이미지를 유지하고, 잘못된 음악은 부분 파일 없이 편집기 오류 안내로 복구한다.
- `SM-F968N` 데이터 보존 설치에서 초기 invalid-font 시험은 Android Typeface의 느슨한 검증을, 다음 valid-font 시험은 시험 APK/대상 APK asset context 차이를 각각 드러냈다. 제품 검증과 시험 context를 수정한 세 번째 전체 실행에서 foreground service 포함 계측 13개가 통과했다. JVM 단위 시험은 18개가 통과했다.
- 편집 세부 기본값·삭제 확인은 `구현됨-미검증`으로 올린다. 글자 1.3배의 실제 확인창 잘림, 실제 사용자 TTF/OTF 다양성, 장시간 외부 음악 export는 사람 AT가 남는다.

## 2026-08-12 구형 혼합 프로젝트·컬렉션 상한 fixture 검증

- schema v1 혼합 프로젝트 fixture에 사진, 영상 원본, 선택 해제된 영상 자클립, 모션 포토, 비슷한 사진 대표·자클립을 함께 넣었다. 구형 JSON을 읽고 현재 schema로 저장한 뒤 다시 읽어도 6개 클립의 정확한 순서, 영상 자클립 선택 상태, Live Photo 표시와 유사 사진 그룹·대표 상태가 유지됐다.
- schema v4 컬렉션 fixture는 실제 앱 저장소에 30개 영화 파일과 사용자 제목, 고정 시각이 다른 5개 고정 영화를 구성했다. 로드 후 30번째 제목을 변경하고 index를 다시 읽어도 30개 상한, 최신 고정 순서, 고정 5개와 변경한 제목이 유지됐다.
- 압축 출력은 원본과 분리된 `.compression-*.tmp.mp4`에 기록하고 크기·영상 트랙·재생 시간을 검증한 뒤에만 최종 파일로 승격한다. 정상 index가 확인된 프로세스 시작에서는 중단 후 남은 이 staging 패턴만 정리하며 원본이나 참조된 압축본은 건드리지 않는다. 전용 fixture에서 부분 staging 제거, 원본 byte 보존, 사용자 제목·pin의 index 재저장·재로드를 확인했다.
- 가져온 글꼴·사용자 copyright 아이콘 fixture는 실제 TTF와 PNG를 연결해 프로젝트로 저장했다. 사용자 아이콘은 전역 선택 파일이 아니라 프로젝트 `media` 안의 원자 교체 복사본을 참조하므로 이후 다른 아이콘을 가져와도 이전 프로젝트 hash가 유지된다. 가져온 글꼴 파일을 의도적으로 제거한 뒤에도 프로젝트의 font ID·아이콘·메모 재저장과 기본 글꼴 fallback이 유지됐다. 같은 확장자의 외부 배경 음악을 바꿀 때도 기존 프로젝트 복사본에 고착되지 않고 검증된 새 파일로 원자 교체한다.
- `SM-F968N`에 제품·테스트 APK를 데이터 보존 설치해 foreground service 회귀를 포함한 계측 시험 17개가 모두 통과했다. fixture는 테스트 APK asset에서 읽고 제품 앱의 실제 `SharedPreferences`·파일 저장소를 왕복하므로 완료 표기가 아닌 저장 구현 근거로 사용한다. `SM-T500`에는 명령을 보내지 않았다.
- 이 시험은 마이그레이션과 컬렉션 데이터 보존 근거를 보강하지만 실제 구버전 앱에서 생성한 대용량 미디어 파일 자체의 장시간 재생·export AT를 대신하지 않는다.

## 2026-08-12 클립 미리보기 파형 보강

- 영상·모션포토 클립 미리보기 정보 패널에 오디오 파형, 현재 완성본 선택 구간, 최대 12개 타격점 표식을 추가했다. 원본 영상은 전체 구간을, 자클립은 실제 trim 구간을 강조하며 기존 `편집` 버튼은 같은 데이터의 타격점 중심 맞춤·구간 미세 조정 화면으로 이어진다.
- 재생, 한 번·반복·자동 다음, 처음·이전·다음, 삭제 확인 동작은 변경하지 않았다. 이로써 명세의 preview waveform/peak 구조는 구현됐으며 작은 화면·글자 확대에서 정보 패널 밀도는 사람 AT가 남아 `구현됨-미검증`으로 분류한다.
- 컬렉션 `ADD A FILM` 포스터는 장식용 영문 조각을 각각 읽지 않고 카드 전체를 `컬렉션에 영화 추가`, 진행 중에는 `컬렉션 영화 가져오는 중`으로 읽는다. 기존 컬렉션 핀의 고정·해제와 고정 순서 drag 설명은 코드 대조에서 이미 명세와 일치해 변경하지 않았다.
- 저장 프로젝트 용량은 메타데이터·썸네일만이 아니라 프로젝트 디렉터리의 원본 복사본, 배경 음악, 사용자 아이콘을 포함한 실제 파일 전체를 합산한다. 저장·삭제 정책은 바꾸지 않고 홈의 사용자 관리 정보만 iOS `directoryByteCount` 의미와 맞췄다.
- 일반 프로젝트 저장은 decode되지 않은 디렉터리를 orphan으로 추정해 삭제하지 않는다. primary·backup 메타데이터가 모두 손상돼 목록에서 잠시 보이지 않는 프로젝트도 내부 미디어를 그대로 보존하며, 사용자 명시 삭제와 저장 성공 후 계산된 10개·AiShot 2개 상한 초과 ID만 정리한다.
- draft나 저장 프로젝트를 열 때 가져온 글꼴·사용자 아이콘 파일이 사라졌으면 기존 기본 글꼴·기본 custom 아이콘 fallback을 유지하면서 설정에서 다시 선택할 수 있다는 복구 안내를 표시한다. 누락 자산 때문에 프로젝트 진입이나 나머지 편집을 막지 않는다.
- 제작 시작 시 프로젝트 ID와 export token만 작은 recovery marker로 남기고 성공·실패·사용자 취소 때 같은 token으로 지운다. 프로세스가 종료돼 marker가 남으면 같은 프로젝트를 다시 열 때 한 번만 중단 안내와 재시도 가능 상태를 표시하며, 클립·설정은 기존 프로젝트 저장소에서 그대로 복원한다. 오래된 token은 새 제작 marker를 지우지 못한다.
- 저장 프로젝트의 파일 기반 클립 원본이나 사용자 배경음악이 외부 정리·손상으로 사라졌으면 프로젝트를 삭제하거나 누락 항목을 조용히 제거하지 않는다. 진입 즉시 누락 개수와 종류를 안내하고, 타임라인·설정·음악 제목을 유지해 사용자가 해당 미디어만 다시 추가하거나 선택할 수 있게 한다. 샘플·리소스 URI는 로컬 파일 누락 판정에서 제외한다.
- 미디어 가져오기는 알려진 원본 크기와 64MiB 안전 여유를, 완성본 만들기는 해상도·fps·전체 길이 기반 예상 출력 크기와 같은 안전 여유를 실제 작업 전에 확인한다. 부족하면 필요한 용량과 현재 사용 가능 용량을 안내하고 기존 클립·설정·프로젝트·컬렉션을 변경하지 않은 채 재시도 상태로 돌아간다.
- 완성본은 캐시의 최종 `.mp4`에 직접 쓰지 않고 `.partial.mp4`에 기록한다. 빠른 단일 영상 mux와 일반 Transformer 모두 영상 트랙·재생시간 검증 뒤 같은 디렉터리에서 원자 승격하며, 다음 제작 시작은 프로세스 종료로 남은 partial만 정리하고 검증된 기존 완성본은 보존한다.
- 날짜별 사진첩 권한 안내에서 앱 설정을 열어 전체 사진·영상 접근을 허용하고 돌아오면 lifecycle resume에서 권한을 다시 읽고 선택기를 이어서 연다. AiShot도 설정 복귀 시 카메라·마이크 권한을 다시 읽어 허용됐으면 권한 패널에서 카메라 preview로 자동 전환한다. 거절 상태에서는 재요청·설정 버튼을 그대로 유지한다.
- 공유 inbox의 사진·영상 URI 묶음은 편집기로 전달을 시작한 뒤 activity 입력 상태를 비우고 처리 signature를 재설정한다. 앱을 종료하지 않고 동일한 URI 묶음을 다시 공유해도 새 intent 이벤트로 다시 처리하며, 한 번의 선택 안에서 기존 중복 제외 규칙은 그대로 유지한다.
- 미디어 원본·Motion Photo 추출은 기존처럼 `.tmp` staging을 확정 파일로 바꾼다. 프로세스가 복사 중 종료되면 다음 앱 cold start에서 `working-media` 바로 아래의 `.tmp`만 정리하고, 확정된 원본과 프로젝트별 복사본은 건드리지 않는다. 실행 중 import에서는 정리하지 않아 취소 직후 새 import와 경합하지 않는다.
- 가져온 글꼴·외부 배경음악·사용자 아이콘도 프로세스 종료로 숨김 staging이 남으면 다음 cold start에서 각 저장소의 정확한 staging 접두사만 정리한다. 확정된 `.ttf/.otf`, `hanclip-music-*`, `custom-icon`과 프로젝트 내부 자산은 보존한다.
- 편집 프로젝트 저장 중 프로세스가 종료돼 남은 `project.json.tmp`와 프로젝트 `media`의 `*.tmp-*`도 cold start에서 정리한다. `project.json`, 검증된 `.bak`, 확정 원본·썸네일·배경음악·사용자 아이콘은 그대로 보존해 backup 복구와 실제 프로젝트 용량 계산을 방해하지 않게 한다.
- 컬렉션 원본 가져오기는 알려진 원본 크기+안전 여유를, 단건·일괄 압축은 기존 해상도·bitrate·길이 기반 예상 결과 크기+안전 여유를 실제 쓰기 전에 확인한다. 부족하면 destination이나 compression staging을 만들기 전에 중단하므로 기존 index·원본·poster·title·pin을 그대로 유지한다.
- 저장 공간 부족은 일반적인 읽기 실패 개수에 섞지 않는다. 컬렉션 다중 import와 일괄 압축도 첫 용량 부족에서 남은 작업을 중단하고 필요한/가용 MB를 그대로 안내하며, 앞에서 완료된 항목과 기존 컬렉션은 유지한다.
- 컬렉션 원본 import도 최종 `<id>.<ext>`에 직접 쓰지 않고 `.import-<id>.tmp.<ext>`에서 복사·hash·메타데이터·poster 준비를 마친 뒤 최종 이름으로 승격하고 index를 저장한다. 정상 index를 읽은 시작에서는 중단 import staging만 압축 staging과 함께 정리하며 index 밖의 일반 파일은 orphan으로 추정해 삭제하지 않는다.
- 첫 컬렉션 영화의 import 도중 종료돼 index가 아직 없는 경우에도 앱 전용 `.import-*` staging은 다음 목록 로드에서 정리한다. 압축 staging은 기존처럼 정상 primary/backup index가 확인된 경우에만 정리해 손상 index 주변의 일반 파일을 추정 삭제하지 않는다.
- 컬렉션 index JSON을 fsync·decode 검증한 뒤 최종 rename 전에 프로세스가 종료돼 유효한 `collection.json.tmp`만 남은 경우, 다음 목록 로드에서 primary가 유효하면 temp를 폐기하고 primary가 없거나 손상됐으면 temp를 primary로 승격한다. 손상 temp는 폐기하며 기존 유효 primary를 덮지 않는다.
- 컬렉션 포스터도 최종 `<id>.jpg`에 직접 쓰지 않고 `.import-<id>.poster.tmp.jpg`에 만든 뒤 영상과 함께 확정한다. 생성 실패·취소에서는 임시 포스터를 지우고 프로세스 중단으로 남은 전용 staging은 다음 목록 로드에서 정리해 완료되지 않은 가져오기의 포스터가 확정 자산처럼 남지 않는다.
- 사진 선택기의 `추가순`은 고정 iOS 소스의 `PHAsset.modificationDate ?? creationDate`에 대응하도록 Android `DATE_MODIFIED → DATE_ADDED → 촬영시각` 순으로 정렬 시각을 해석한다. 촬영순과 달력 월 필터는 기존 촬영시각 기준을 유지하며 단위시험으로 우선순위와 두 fallback을 고정했다.
- 저장 프로젝트를 읽을 때 사진 길이는 0.1~30초로, 영상·모션 trim은 음수가 아니고 원본 길이를 넘지 않도록 정규화한다. 정상 저장값은 보존하며 손상·구형 JSON의 음수, 과도한 길이, 원본 밖 시작점만 안전 범위로 복구하는 단위시험을 추가했다.
- 구형 초안에 `projectId`가 없으면 UUID를 한 번 생성해 즉시 초안 JSON에 기록하고, 파일 저장 프로젝트에서 누락됐으면 기존 디렉터리명을 안정 ID로 채워 원자 저장한다. 반복 로드에서도 같은 ID와 pin·memo가 유지되는 계측 시험으로 프로젝트 식별자가 세션마다 바뀌지 않음을 확인한다.
- AiShot 새 세션의 첫 녹화 전에는 이전 프로세스·세션에서 남은 정확한 `aishot-buffer-<시각>.mp4`와 `aishot-<순번>-<시각>.mp4` cache 파일만 정리한다. 현재 세션 파일 생성 전에 한 번만 실행하며 비슷한 이름이나 일반 cache 영상은 보존하는 단위시험을 추가했다. trigger 전후 계산은 계속 monotonic `elapsedRealtime`을 사용한다.
- 컬렉션 index의 `movies`가 없거나 배열이 아닌 경우 유효한 빈 컬렉션으로 오인하지 않고 손상 primary로 판정해 정상 backup을 읽는다. 배열 안의 개별 손상 항목만 격리하는 기존 정책은 유지하며, 의미상 손상된 primary와 정상 v1 backup fixture로 silent reset 방지를 확인했다.
- 저장 프로젝트 metadata도 문법상 JSON이라는 이유만으로 primary를 채택하지 않고 `project` 객체 구조까지 확인한다. `project`가 문자열로 손상된 primary가 정상 `.bak`을 가리지 않는 기존 backup 회귀 시험으로 프로젝트가 홈에서 조용히 사라지는 경계를 막았다.
- 미디어 선택 Dialog의 표시·종류와 내부 월·선택 날짜/URI·정렬·필터를 `rememberSaveable`로 옮겼다. 회전·폴드뿐 아니라 saved-state가 제공되는 프로세스 재생성에서도 선택 세션을 복원하며, preview·drag·가져오기 확인창 같은 순간 상태는 완료로 오인되지 않도록 복원하지 않는다. 월·날짜·영상 길이 필터 codec 경계를 단위시험으로 고정했다.
- 저장 프로젝트나 초안의 모션포토 영상 파일이 사라졌지만 짝 사진이 남아 있으면 해당 클립을 삭제하거나 일반 누락 원본으로 남기지 않고 사진 모드로 복구한다. 클립 ID·순서·길이·편집 설정을 유지하며 진입 안내로 대체 사실을 알리고, 일반 사진·영상 및 사진까지 사라진 모션포토는 기존 누락 미디어 경고를 유지한다.
- 모션포토가 `영상` 모드이면 클립 행과 미리보기의 `편집`이 사진 길이가 아니라 영상 구간 선택으로 열린다. trim과 ±시간 조절은 모션 원본 길이 안에서 보정하고, `사진` 모드로 전환하면 기존 사진 길이 편집을 유지한다.
- 새로 가져온 모션포토는 고정 iOS 기준처럼 `영상` 모드와 추출된 motion 영상의 실제 길이로 시작하고, 사진 길이는 프로젝트 기본시간으로 별도 보존한다. motion 추출이 없거나 길이가 유효하지 않으면 일반 사진 모드와 양수 길이로 안전하게 fallback한다.
- 영상 길이의 `전체영상`은 각 원본 영상의 자클립을 하나의 원본 전체 구간으로 안전하게 합치고 사진·모션포토·묶음 길이를 변경하지 않는다. `선택구간`과 `전체영상` 안내 개수도 자클립 수가 아니라 원본 영상 수를 사용한다.
- `기본시간` 전체 적용에서 영상 모드 모션포토는 고정 iOS처럼 motion 원본 길이 안에서 기본시간을 clamp하고 가운데 구간을 선택한다. 사진 모드 모션포토는 기존 사진 기본시간과 0초 trim을 유지한다.
- `전체영상` 전환 전 원본 영상·자클립의 사용자 선택구간을 프로젝트 ID별 세션 스냅샷으로 보존한다. 전체 모드에서는 자클립을 삭제하지 않고 결과에서만 제외하며 `선택구간`으로 돌아오면 각 trim·분할·사용 상태를 정확히 복원하고, 전환 사이 추가된 다른 ID의 클립은 유지한다.
- 개별·일괄 모션포토를 `사진→영상`으로 전환하면 motion 원본 전체를 무조건 쓰지 않고 현재 기본시간을 원본 길이 안에서 clamp한 가운데 구간을 선택한다. 다시 사진으로 돌아갈 때 쓸 `photoDuration`은 영상 전환으로 덮지 않는다.
- 클립 trim·사진 길이·미리보기 Dialog의 대상 ID와 적용 전 시간 초안을 `rememberSaveable`로 보존한다. 회전·접힘 재구성 뒤 같은 클립 편집 화면과 조정값을 유지하되 삭제·초기화 확인창 같은 순간 확인 상태는 복원하지 않는다.
- 편집기의 `클립 설정`·`고급 설정` 펼침과 `순서 변경` 활성 상태도 편집 View 세션 동안 회전·접힘을 견디도록 보존한다. 새 프로젝트는 기존 기본값대로 클립 설정이 닫힌 상태에서 시작하고 영구 프로젝트 JSON에는 UI 상태를 추가하지 않는다.
- 음악 설정 화면과 진입 당시 음량·원본음량·반복·fade 되돌리기 기준을 구성 변경에도 보존한다. 음악 미리보기의 재생 대상과 Player는 순간 상태로 복원하지 않으며, 퀵모드에서 설정을 닫을 때 돌아갈 흐름만 함께 유지한다.
- 빠른 길이 설정은 구성 변경 뒤에도 창 표시, 선택 시간, 추천 선택 상태와 미디어 선택기 복귀 흐름을 보존한다. 새 편집 세션에서는 기존 프로젝트 ID 조건에 따라 추천 길이로 초기화한다.
- 공통 전체화면 플레이어의 화면 맞춤은 iOS 고정 소스의 `.automatic` 계약처럼 유효한 영상 크기가 가로일 때 가로 viewport에서만 채우기를 기본 적용한다. 사용자가 fit/fill을 선택하면 자동 판단을 중단하며, 세로 viewport와 크기 미확인 영상은 안전하게 fit을 유지한다.
- 자막·엔딩 설정 화면과 적용 전 `WatermarkSettings` 초안은 구성 변경 뒤에도 보존한다. 적용·취소·되돌리기 의미와 프로젝트 JSON 형식은 바꾸지 않고, Bundle 저장용 직렬화 버전만 명시한다.
- 전체화면 플레이어의 공통 컨트롤은 실제 재생 중에만 자동으로 숨고, 버퍼링과 반복 해제 후 재생 종료에서는 다시 표시한다. 반복 재생·seek·PlayerView 제어는 기존 Media3 동작을 유지한다.
- 시사회에서 전체화면으로 전환하면 인라인 Player의 현재 위치를 시작 시각으로 전달하고 인라인 재생을 멈춰 이중 오디오를 막는다. 전체화면을 닫으면 기존 인라인 Player를 다시 재생하며, 컬렉션 직접 재생은 기존처럼 0초에서 시작한다.
- 전체화면 컨트롤이 숨겨진 동안 공통 Player 위치를 따르는 2dp 하단 미니 진행선을 표시한다. duration 미확인 상태는 0으로 처리하고 Dialog 종료 시 위치 관찰 coroutine도 함께 취소한다.
- 고정 iOS `Info.plist`와 Android `shortcuts.xml`은 홈 아이콘에 사진·퀵모드·AiShot 세 개만 노출한다. 양쪽 라우터는 별도 외부 URL용 `open/calendar/files/search`까지 모두 유지하므로 노출 목록과 라우팅 목록의 차이는 의도된 동일 동작으로 확인했다.
- 실제 `SM-F968N` 닫힌 커버 화면에서 다크 테마 상태바 아이콘이 검정색으로 고정돼 보이지 않는 문제를 확인했다. 홈·편집·시사회·음악 브라우저는 현재 테마 panel 명도, AiShot은 검정 배경에 맞춰 Activity 시스템 바 아이콘과 내비게이션 바를 갱신하며 전체화면 Dialog의 별도 window 정책은 유지한다.
- 전체화면 Media3 진행바의 scrub 시작·종료를 공통 컨트롤 상태에 연결한다. scrub 중에는 컨트롤 자동 숨김을 멈추고, 손을 놓은 뒤 실제 재생 중일 때만 기존 약 3초 숨김 타이머를 다시 시작하며 인라인 시사회 PlayerView에는 영향을 주지 않는다.
- 전체화면 Player는 Activity lifecycle `ON_STOP`에서 현재 위치를 유지한 채 pause하고, 그때 실제 재생 중이었던 동일 Dialog만 `ON_START`에서 resume한다. 사용자가 직접 일시정지한 상태와 loop/fit/zoom 등 Compose 세션 상태는 그대로 보존하며 dispose 시 observer를 제거한다.
- 기본 사진첩의 `오늘`은 고정 iOS 계약처럼 첫 탭에서 오늘 섹션으로 이동하고 선택 준비 상태를 표시하며, 같은 상태의 두 번째 탭에서 오늘 미디어만 선택한다. 필터·정렬·전날·해제·달력 전환·사용자 drag는 준비 상태를 해제하고, 오늘 미디어가 없으면 최신 날짜로만 이동한다. 세 상태 전이는 전용 단위시험으로 고정했다.
- 공통 전체화면 영상 표면의 단일 탭은 고정 iOS 소스처럼 재생/일시정지를 전환하고 컨트롤을 드러낸다. 재생 종료 상태에서 탭하면 0초로 되감아 다시 재생하며, 좌우 double tap ±10초와 확대 상태 double tap 초기화는 기존 동작을 유지한다. PlayerView에는 `시사회 재생 또는 일시정지` 접근성 설명을 추가했다.
- 공통 전체화면의 fit/fill 버튼은 고정 iOS처럼 가로 viewport에서만 표시한다. 세로·정사각 viewport는 자동 fit/fill 판단을 유지하되 불필요한 수동 토글을 숨기며, 회전으로 다시 가로가 되면 버튼이 복원된다.
- 공통 전체화면 컨트롤은 고정 iOS와 Media3 진행바에 맞춰 실제 재생 중 마지막 조작 후 3초에 숨긴다. scrub·일시정지·버퍼링 중에는 기존처럼 숨김을 중단한다.
- 공통 전체화면의 상단 loop/fit/share/close와 Media3 재생·진행 컨트롤은 하나의 표시 상태를 사용한다. 단일 탭·scrub·버퍼링에서 함께 나타나고 3초 뒤 함께 숨으며, 인라인 시사회의 Media3 자동 컨트롤은 기존 동작을 유지한다.
- 공통 전체화면의 loop/fit/share/close 원형 버튼은 Android 최소 터치 높이 48dp를 확보한다. 아이콘·간격·동작은 유지한다.
- 음악 설정의 샘플 미리듣기·브라우저·파일 불러오기와 브라우저 다운로드 취소 조작은 Android 최소 48dp 터치 영역을 확보한다. 음악 선택·미리듣기·다운로드 취소 동작은 변경하지 않는다.
- 컬렉션 포스터 메뉴와 일괄 압축 해상도 버튼도 Android 최소 48dp 터치 영역을 확보한다. 포스터 제목 상단 여백만 메뉴 높이에 맞추고 핀·메뉴·압축 동작과 저장 데이터는 변경하지 않는다.
- 사진·영상 접근을 이미 허용한 사용자는 선택적 위치 메타데이터 권한을 거절했어도 날짜별 선택기를 바로 연다. 위치 권한창을 재진입마다 반복하지 않으며, 위치를 읽을 수 없는 미디어는 기존 무위치 fallback으로 안전하게 가져온다.
- 퀵모드 화면비와 편집 설정의 공통 선택칩은 Android 최소 48dp 터치 높이를 확보한다. 선택 값·적용 범위·프로젝트 저장 형식은 변경하지 않는다.
- 엔딩 시간, 편집 설정 수치, 클립 시간의 공통 −/+ 조작도 Android 최소 48dp 터치 영역을 확보한다. 기존 step·최솟값·최댓값·저장 동작은 유지한다.
- 영상 구간 편집의 길이 프리셋과 시작·길이 미세 조정 버튼도 Android 최소 48dp 터치 높이를 확보한다. trim 범위·step·타격점 중심 맞춤 계산은 변경하지 않는다.
- AiShot의 닫기·저장 클립 편집·줌 배율·감도 필터 조작은 Android 최소 48dp 터치 높이를 확보한다. 카메라 preview·녹화·감지·저장 순서는 변경하지 않는다.
- 엔딩 테마·시간과 자막 날짜 삽입·선택 세그먼트·스타일 프리셋도 Android 최소 48dp 터치 높이를 확보한다. 자막·엔딩 설정값과 실제 합성 렌더링은 변경하지 않는다.
- 미디어 선택기의 달력 날짜·선택 해제와 영상 정렬·시간 필터·전체 선택 조작도 Android 최소 48dp 터치 높이를 확보한다. 선택 순서·필터·정렬 상태는 변경하지 않는다.

## 2026-08-12 잔여 접근성·사진 전환 동기화

- 고정 iOS 기준 `31e60ec5feb100b3dfcec78d47040e54e2d682ca`의 48dp Android 계약을 홈 i 버튼·워터마크·화면 꺼짐 방지, 프로젝트 메모·핀, 달력 월 이동, 텍스트 위치·서체, 클립 순서·삭제와 설정 행까지 확장했다. 작은 시각 요소는 유지하고 실제 탭 영역만 넓혔다.
- 사진과 달력은 같은 선택 URI·정렬·필터 상태를 유지하면서 고정 iOS 수치와 같은 0.11초 교차 전환을 사용한다. 오늘 2단계 선택과 달력 전환 시 준비 상태 해제는 기존 단위시험을 유지한다.
- 편집·시사회 로고, 달력 전환, 전체 서체 화살표에 행동 라벨을 추가했다. 홈·자막·편집의 공용 선택 컨트롤과 달력 날짜·필터·미디어 썸네일은 선택 여부와 순서를 접근성 트리에 노출한다.
- 전체화면 반복·맞춤·공유·닫기 버튼은 전달받던 행동 설명을 실제 접근성 트리에 연결하고, 반복·맞춤은 현재 선택 상태도 함께 노출한다. 재생·공유·닫기 콜백과 표시 상태는 변경하지 않았다.
- 각 소스 묶음은 `testDebugUnitTest`, `assembleDebug`, `assembleDebugAndroidTest`, `lintDebug`, `assembleReleaseQa`를 통과한 뒤 `SM-F968N`에 `adb install -r`로 설치하고 프로세스 실행을 확인했다. `SM-T500`에는 명령을 보내지 않았고 사용자 `PROJECT_RULES.md`는 커밋에서 제외했다.
- `SM-F968N` 822×1918·글자 1.15배·사진/영상 권한 허용 상태에서 사진→달력→사진 왕복과 선택 유지, 취소 후 저장 없는 홈 복귀를 확인했다. 오늘 첫 탭은 `0개 추가`를 유지하고 두 번째 탭은 `1개 추가`로 바뀌어 2단계 선택 AT를 통과했다.
- 같은 기기에서 시스템 글자 배율을 1.3으로 잠시 변경해 홈 3×2 프리셋·영화 목록·컬렉션·미디어 추가·i 버튼과 편집 상단·미디어 추가·하단 닫기 영역의 잘림이 없음을 확인한 뒤 원래 1.15로 복원했다. 앱 데이터와 선택은 저장하지 않았다.
- 달력 셀을 48dp로 확장한 뒤 격자 컨테이너가 예전 42dp 행 높이를 유지해 6번째 행의 30·31일이 절반 잘리는 실기기 회귀를 발견했다. 컨테이너와 빈 셀도 행당 48dp로 맞추고 같은 822×1918 화면에서 30·31일 셀 모두 96px 전체 높이로 표시됨을 재확인했다.
- 미디어 썸네일은 파일명·선택 여부·선택 순서·클릭·길게 누르기를 하나의 접근성 버튼으로 병합했다. `SM-F968N` UI 트리에서 선택한 `hanclip-home.png`가 별도 자식 없이 `checked=true`, 클릭·길게 누르기 가능한 단일 버튼으로 노출되고 `1개 추가` 상태가 함께 갱신됨을 확인했으며 선택은 저장하지 않고 닫았다.
- 컬렉션 추가 포스터도 장식용 영문 자식과 클릭 부모가 분리되지 않도록 의미를 하나로 고정했다. `SM-F968N` UI 트리에서 `컬렉션에 영화 추가`가 자식 없는 단일 `android.widget.Button`, `clickable=true`로 노출됨을 확인했다.
- 홈 하단 카피라이터 `i`도 설명 자식과 클릭 부모를 하나로 합쳤다. 같은 기기에서 `카피라이터 설정, 길게 눌러 음악 브라우저 열기`가 단일 항목에 `clickable=true`, `long-clickable=true`로 노출됨을 확인했다.
- 같은 설치본의 홈·빈 편집·미디어 1개 선택 화면에서 `clickable=true`이면서 자체 텍스트·설명·설명 자식이 모두 없는 노드는 0개였다. 미디어 선택은 저장하지 않고 홈으로 복귀했다.
- 정적 터치 영역 감사에서 남은 순서 변경 `완료`와 컬렉션 `용량 줄이기` 조작을 48dp로 확대했다. 같은 필수 검증 묶음을 통과하고 `SM-F968N`에 데이터 보존 설치해 앱 실행과 기존 홈·빈 편집 화면 배치를 확인했다.
- `SM-F968N`을 복원 가능한 device state 3으로 잠시 전환해 실제 2160×1584·320dpi 펼침 화면을 감사했다. 홈이 3열로 전환됐지만 `fillMaxSize`가 `widthIn(max = 920.dp)`보다 먼저 적용돼 핵심 영역이 2104px(1052dp)까지 늘어나는 결함을 발견했고, 홈·편집의 수정자 순서를 바꿔 920dp 제한을 먼저 적용했다. 필수 검증과 데이터 보존 설치는 통과했으나 설치 뒤 펼침 화면이 보안 잠금으로 전환돼 수정 후 사람 화면 재측정은 하지 않았으며, 자세 override는 실제 닫힘 상태 0으로 복원했다.
- 실제 TalkBack 음성 탐색은 사람 AT가 남아 있으므로 접근성 전체 항목은 `구현됨-미검증`으로 유지한다.

## 2026-08-12 추가 3% 잔여 실기기 감사

- 고정 iOS 기준은 `31e60ec5feb100b3dfcec78d47040e54e2d682ca`로 다시 확인했다. 홈·편집 루트는 920dp, 테마 패널은 620dp 상한을 코드에서 확인했고 reset/exit 확인과 공유 inbox는 Android 기본 대화상자 폭이 각각 760dp·720dp보다 작아 상한 계약을 넘지 않는다.
- 공통 전체화면 플레이어는 `smallestScreenWidthDp < 600`인 phone에서만 full-sensor 방향 정책을 적용하고, 펼친 화면은 현재 window 방향을 유지한다. fit/fill·세로 닫기·확대 이동·±10초·수명주기 일시정지는 공통 구현과 단위시험 근거가 있으나 잠긴 실기기의 사람 제스처 검수는 수행하지 않았다.
- AiShot은 `OrientationEventListener`의 물리 방향을 CameraX preview와 capture target rotation에 함께 반영하고 렌즈 재결합 때 현재 display rotation으로 초기화한다. 저장 MP4의 실제 회전 metadata·프레임 확인은 잠긴 실기기에서 실행하지 않아 `구현됨-미검증`을 유지한다.
- 장시간 제작은 foreground service 알림·진행률·동일 token 취소 handshake와 계측시험 근거가 있다. 실제 Transformer 작업은 ViewModel coroutine이 소유하므로 프로세스 종료 뒤 이어서 제작하지 않고 recovery marker로 안전하게 재시도한다. 따라서 장시간 혼합 export의 화면 이탈·알림 취소·완료 AT는 계속 `구현됨-미검증`이며, 서비스로 작업 소유권을 옮기는 변경은 별도 대형 작업으로 분리한다.
- 고정 기본값 재대조에서 자동 날짜 자막을 iOS의 `yy. M. d. (EEE)` 형식으로 통일하고, 촬영기간은 두 번째 날짜가 줄 중간에서 갈라지지 않도록 동일한 non-breaking space 규칙을 적용했다. 여행 프리셋은 가져오기 완료 뒤 촬영기간과 촬영 빈도가 높은 지역 최대 두 곳으로 자막을 갱신하며, 보물지도 엔딩은 기존처럼 자동 활성화하지 않는다. 날짜 단일·범위 형식은 JVM 단위시험으로 고정하고 실제 지역명이 포함된 여행 자막 화면은 잠금 해제 뒤 사람 AT로 남긴다.
- 기본시간 증감 버튼은 iOS 허용값 순서와 동일하게 0.1~1.0초는 0.1초, 1.5~10초는 0.5초, 11~30초는 1초 간격으로 이동한다. 구형 저장값이 허용값 사이에 있어도 증감 방향의 가장 가까운 허용값으로 이동하며 최소·최대와 세 구간 경계를 JVM 시험으로 고정했다.
- 개별 사진 시간은 빠른 `-1초/+1초` 조작에서도 0.1~30초를 벗어나지 않으며, 영상·모션포토는 원본 길이를 상한으로 사용한다. 편집 요약과 자막 화면의 엔딩 시간 버튼도 1~10초 경계에서 비활성화해 표시값과 내부 저장값이 어긋나지 않게 했다.
- 사진 선택 필터는 `사진/모션포토/영상`을 하나 이상 자유롭게 조합하고 `전체`로 즉시 복원한다. 마지막 한 종류는 해제되지 않으며 영상 길이 필터가 켜질 때 영상만 사용했다가 해제 시 이전 복수 조합을 복원한다. 조합·해제·최소 1종·전체 복원은 JVM 정책 시험으로 고정했다.
- 영상 길이 필터가 켜진 동안은 iOS처럼 사진·모션포토·영상 개별 전환을 잠그고 `전체` 또는 길이 필터 해제로만 빠져나간다. 촬영일/추가일 정렬은 같은 기준 재선택 때 방향을 반전하고 다른 기준으로 바꿀 때 기존 오름·내림 방향을 유지하도록 정책 시험으로 고정했다.
- `SM-F968N`은 앱 1.0.1(522)이 설치되어 있고 닫힘 자세 0이지만 보안 잠금 상태다. 잠금 해제나 인증 입력을 시도하지 않았고 `SM-T500`에는 시험 명령을 보내지 않았다.
- AiShot 중앙 수동 촬영 버튼에 정지 상태 `AiShot 수동 촬영`, 저장 중 상태 `AiShot 클립 저장 중지` 행동 설명을 추가했다. 카메라·감지·녹화·저장 순서는 변경하지 않았고 필수 빌드·시험·린트 통과 뒤 `SM-F968N`에 데이터 보존 설치와 프로세스 실행을 확인했다.
- AiShot 감도 4종과 줌 배율은 색상뿐 아니라 선택 상태를 접근성 트리에 노출한다. 샷 시간은 현재 단계와 타격 전·후 시간을, 카메라 전환은 현재 전면·후면 상태를 행동 설명으로 제공하며 같은 필수 검증과 데이터 보존 설치를 통과했다.
- AiShot 클립 저장 막대는 진행 퍼센트와 남은 초를 접근성 상태로 제공한다. 실제 저장 시간 계산과 화면 막대는 변경하지 않았다.
- 엔딩 사용 여부·5종 테마·자막형 스타일, 영상 trim 길이 프리셋, 음악 샘플, Quick 화면비도 색상과 기호만이 아니라 radio/선택 상태를 접근성 트리에 노출한다. 각 선택의 적용·미리보기·렌더·프로젝트 저장 로직은 변경하지 않았다.
- 자막과 카피라이터의 5×5 위치 선택은 25칸 각각의 행·열 위치를 읽는다. 엔딩 미리보기는 장식 문자를 따로 읽지 않고 테마·촬영기간·방문 경로를 한 설명으로 제공한다. custom 테마 순서는 물리 drag와 함께 경계에 맞는 `위로 이동`·`아래로 이동` 접근성 동작을 제공한다. 좌표·테마 순서 저장 형식은 유지한다.
- 영상 구간과 클립 미리보기의 오디오 파형은 현재 선택 시작·끝 시각과 타격점 개수를 읽는다. 파형 분석·trim·재생 동작은 변경하지 않았다.

## 2026-08-12 편집 취소 자산 보존

- 같은 확장자의 배경음악이나 사용자 카피라이터 이미지를 편집 중 교체해도 이전 프로젝트 파일을 덮어쓰지 않고 프로젝트 내부의 고유 파일로 확정한다. `홈` 취소는 세션 시작점이 참조한 이전 음악·이미지 bytes를 다시 가리키며, `저장`은 새 자산을 기준점으로 삼는다.
- 이전 자산은 project metadata backup과 편집 취소 기준이 참조할 수 있어 프로젝트가 명시 삭제될 때까지 보존한다. 앱 전역 원본·다른 프로젝트·클립·핀·메모는 변경하지 않는다.
- 전체 필수 검증 통과 뒤 `SM-F968N`에 제품·테스트 APK를 데이터 보존 설치했다. 같은 확장자의 음악·이미지를 각각 교체한 뒤 이전 bytes와 새 bytes가 모두 유지되고 세션 시작 프로젝트 복원이 이전 URI를 다시 선택하는 전용 계측시험 2개가 통과했다.

## 2026-08-12 브라우저·내장음악 출시 안전성

- 외부음악 WebView는 사이트 재생과 영상 탐지에 필요한 JavaScript·DOM storage는 유지하되 로컬 file/content URI와 HTTPS 페이지의 혼합 HTTP 하위 콘텐츠 접근을 차단한다. 즐겨찾기·HTTPS 탐색·명시적 다운로드 흐름은 변경하지 않는다.
- 내장음악 6개는 문자열 리소스 이름 대신 빌드가 추적하는 `R.raw` ID에 직접 연결한다. 저장하는 sample ID·화면 제목·실제 오디오 파일은 바꾸지 않으며 출시 최적화에서도 미사용 리소스로 오판되지 않게 한다.
- 필수 검증과 `SM-F968N` 데이터 보존 설치 뒤 6개 sample ID가 패키지의 실제 오디오를 모두 여는 계측시험 1개가 통과했고, lint의 내장음악 `UnusedResources` 4건은 0건이 됐다.
- 외부음악 화면 종료 시 전체화면 video callback을 복원하고 WebView 로딩·JavaScript bridge·download listener·view tree를 해제한 뒤 `destroy()`한다. 시스템 DownloadManager로 이미 시작된 다운로드와 저장된 즐겨찾기는 유지하며 반복 진입의 WebView 누적을 막는다.

## 2026-08-13 iPhone 3.11.44 디자인·편집 흐름 동기화

- iPhone 최종 Swift와 당일 변경을 전수 기술서 `reference/ios-current/design-spec/ANDROID_FULL_PORT_SPEC_2026-08-13.md`로 고정하고 Android P0/P1을 다시 대조했다.
- 전역 Android UI는 시스템 sans 기반 20/17/16/15/13/12sp 계층을 사용한다. 영상·엔딩 결과 미리보기 내부 글자와 하단 `첫\n사진` 10sp는 렌더 시뮬레이션 예외로 유지한다.
- 홈 프리셋은 글자 배율에 따라 계산한 동일 고정 높이, 3/2/1열 적응형, 15sp 제목·13sp 설명 최대 2줄을 사용한다. 빌드 530에서 두 행 여섯 카드의 외곽 크기와 설명 노출을 SM-F968N 캡처로 재확인했다. `영화 프리셋`은 iPhone처럼 왼쪽, 영화 목록·컬렉션은 오른쪽 정렬한다.
- 컬렉션 추가 카드는 MaruBuri 15sp `ADD A FILM`, 9/8sp 장식 문구, 1.2dp·5/4dp 점선을 사용하며 단일 접근성 버튼과 가져오기 동작은 보존한다.
- 편집 일반 행은 56dp/16sp, 자막·음악·엔딩 요약은 82/82/88dp와 16/15/13sp 계층을 사용한다. 묶음사진은 간격과 자동·수동·전체를 2행으로 유지한다.
- 자막은 꺼진 상태에서 설정 진입 시 로컬 초안만 자동 활성화하고, 엔딩은 변경이 있을 때만 저장·취소를 표시한다. 빠른 토글은 전역 적용 알림을 띄우지 않는다.
- 음악 설정의 변경 판정과 취소 기준은 부모 `MusicSettingsSnapshot` 하나를 사용한다. 파일 선택·브라우저 왕복에도 기준을 유지하고 0.001 음량 허용 오차로 변경 시에만 저장 버튼을 표시한다.
- 퀵모드의 초기 안전영역 수정은 충분하지 않았다. 빌드 534~538 실기기에서 만들기 버튼이 `[40,1868][782,1918]`의 50px만 보이는 회귀를 재현했고, 빌드 540에서 고정 오버레이를 제거해 화면 비율 바로 아래 스크롤 항목으로 배치했다. 최종 버튼은 `[40,1630][782,1746]`의 58dp 전체가 보이며, 1.3배에서도 `[40,1677][782,1793]`로 화면 안에 있다.
- 퀵 요약은 사용자 최종 계약에 따라 `자막 : 내용없음`, `음악 : 햇살 한 컷`, `엔딩 : 안함`의 한 줄만 표시한다. 화면 비율은 비선택 상태도 밝은 카드·진한 윤곽, 선택 상태는 진한 배경·흰 아이콘으로 대비를 확보했다. 음악 URI와 내장 sample ID는 모두 선택된 음악으로 인정한다.
- 홈에서 퀵모드를 선택하면 퀵 편집 세션을 먼저 만든 뒤 공용 사진 화면을 자동으로 연다. 사진 선택이 편집 세션에 반영되면 퀵 길이 화면을 한 번 표시하며, 사진 화면을 열기 전 빈 퀵 길이 화면을 먼저 띄우지 않는다.
- 빌드 530에서 사진 그리드 드래그는 iOS `linearIndex` 계약처럼 직전 셀과 현재 셀 사이를 모두 선택·해제한다. 빠르게 행을 넘겨 중간 셀을 건너뛰어도 전체 선형 범위를 포함하는 JVM 시험을 추가했다.
- 사진 첫 표시 전에 모든 원본의 512KB 모션포토 검사를 끝내던 병목을 제거했다. MediaStore 목록을 먼저 노출하고 12개 단위로 모션포토 종류를 보강하며, Android 10 이상은 원본 디코딩보다 시스템 캐시 썸네일을 우선한다. 가져오기 시 실제 모션포토 추출·판별은 유지한다.
- 브라우저 상단·음악 미리듣기·엔딩 증감·AiShot 감도는 Android 48dp 터치 영역을 유지한다. AiShot 감도는 보이는 캡슐만 32dp로 제한해 카메라 화면을 가리지 않는다.
- `testDebugUnitTest`와 `assembleDebug`가 통과했고 `SM-F968N`에 데이터 보존 방식으로 설치했다. 홈 여섯 프리셋의 동일 크기·2줄 설명과 퀵모드 선택 직후 사진 화면 자동 진입을 822×1918에서 확인했다. 이후 실제 기기 미디어로 사진 선택부터 퀵 길이 화면까지 이어지는 경로, 한 줄 요약, 화면 비율 대비와 하단 CTA 전체 노출을 빌드 540에서 직접 확인했다. 사진 첫 목록은 명령 간격 1초 안에 보였지만 정밀 계측값은 아니므로 성능 완료 수치로 사용하지 않는다. `SM-T500`에는 명령을 보내지 않았다.

## 2026-08-14 P2 시각 위계·홈 설명 조정 · Android 빌드 531

- 사진 선택 상단의 취소·사진·추가 세 영역을 각각 최소 88×48dp로 대칭 배치하고 제목을 17/23sp SemiBold로 정리했다. SM-F968N 접근성 트리의 실제 bounds는 각각 176×96px 이상으로 88×48dp를 충족했다.
- 달력 월·날짜, 트림 `편집`, 시사회·개봉, 컬렉션 압축/진행/해상도 옵션의 Black 및 9~11sp 조작 문구를 12~17sp Regular~SemiBold 계층으로 올렸다. 포스터·영상 결과 미리보기 안의 축소 장식 문구는 렌더 시뮬레이션 예외로 유지했다.
- 프리셋 설명은 사용자 지정에 따라 13/18sp의 정확한 80%인 10.4/14.4sp로 줄였다. 제목 15/18sp, 아이콘40dp, 카드 외곽 크기와 3/2/1열 전환은 유지한다. 이 설명 크기는 iPhone 13sp와 의도적으로 다른 Android 사용자 지정이다.
- `assembleDebug`가 통과했고 `SM-F968N`에 데이터 보존 방식으로 1.0.1(531)을 설치했다. 1.15배에서 3열, 임시 1.3배에서 2열로 전환되고 여섯 카드와 설명이 잘리지 않는 것을 캡처로 확인했다. 펼침 상태 3의 2160×1584 화면에서도 920dp 중앙 폭, 3×2 동일 카드와 아래 2열 영화 목록이 잘리지 않았고 즉시 닫힘 상태 0으로 복원했다. 2.0배 확인 때 화면이 자동 잠겨 검은 잠금 화면만 캡처되어 사람 AT로 인정하지 않았으며, 시스템 글자 배율은 원래 1.15로 복원했다. 트림·시사회·압축 화면은 실제 콘텐츠가 없어 이번 설치본에서 사람 AT하지 못했다.

## 2026-08-14 프리셋 내부 균등 배열 · Android 빌드 532

- 첨부한 네이버 로그인 카드의 수직 리듬을 기준으로 프리셋 카드 내부를 같은 높이의 세 영역으로 나누고 아이콘·제목·설명을 각 영역 중앙에 놓았다. 카드 외곽 높이, 40dp 아이콘, 제목 15/18sp, 설명 10.4/14.4sp와 클릭 동작은 유지한다.
- `assembleDebug` 통과 뒤 SM-F968N에 데이터 보존 설치했다. 1.15배 3열과 1.3배 2열 모두 여섯 카드가 같은 크기이며 세 요소 중심선이 고르게 떨어지고 문구가 겹치거나 잘리지 않는 것을 캡처로 확인했다. 시험 뒤 글자 배율은 1.15로 복원하고 앱을 정지했다.

## 2026-08-14 달력 오늘·전날 단계 선택 · Android 빌드 541

- `오늘`은 첫 탭에서 오늘 위치로 이동하고 선택 준비 상태를 강조한다. 같은 상태에서 두 번째 탭하면 오늘 촬영 미디어를 모두 선택한다. 사용자가 날짜·필터·정렬·전날·해제를 조작하면 준비 상태를 해제한다.
- 기본 사진 화면의 `전날`은 선택 미디어가 없으면 어제 촬영 미디어를 선택하고, 선택이 있으면 그중 촬영일이 가장 빠른 날짜의 전날 미디어를 모두 선택한다. 대상 날짜에 미디어가 없으면 기존 선택은 보존하고 해당 날짜로만 이동한다.
- 정책 단위시험 5건과 빌드 541이 통과했다. SM-F968N에서 `오늘` 첫 탭 0개 유지·두 번째 탭 오늘 1개, 이어 `전날` 24개·다시 `전날` 2개를 확인했으며 `추가`는 누르지 않았다.

## 2026-08-14 퀵 저장 선행·사진 드래그 왕복 · Android 빌드 542

- 퀵모드의 `이 시간으로 만들기`는 현재 편집 세션을 영화 목록에 먼저 저장한 뒤 완성본 제작을 시작한다. 저장 실패 시 제작과 시사회 이동을 시작하지 않으므로 홈으로 돌아왔을 때 반쪽짜리 제작 상태가 생기지 않는다. 일반 편집의 만들기도 같은 저장 선행 계약을 사용한다.
- 사진 선택 드래그는 제스처 시작 시점의 선택 목록과 고정 시작 항목을 기준으로 매 이동마다 범위를 다시 계산한다. 앞으로 끌어 선택·해제한 뒤 손가락을 떼지 않고 반대로 돌아오면, 현재 범위에서 빠진 항목은 제스처 전 선택 상태로 돌아가 Google 포토와 같은 왕복 선택을 만든다. 활성 범위 밖의 기존 선택과 가장자리 자동 스크롤은 유지한다.
- 저장→제작 순서와 저장 실패 차단, 선택 드래그의 전진·후퇴·범위 밖 선택 보존을 포함한 관련 정책 시험 8건 및 `assembleDebug`가 통과했다. SM-F968N에서 빌드 542 설치와 퀵모드→사진 자동 진입을 확인하고, 한 제스처로 4칸까지 전진한 뒤 2칸까지 후퇴했을 때 최종 2개만 선택되고 뒤 2개가 해제되는 것을 접근성 트리로 확인했다. 선택은 상단 취소로 폐기했고 추가·저장·제작은 실행하지 않았다.

## 2026-08-14 사진 하단 드래그 자동 스크롤 · Android 빌드 543

- 사진 드래그가 실제로 시작되면 하단 `필터·전날·오늘·해제·추가` 막대를 숨기고 손을 떼거나 취소하면 즉시 복원한다. 고정 버튼 높이도 드래그 자동 스크롤의 가려진 영역으로 반영해 손가락이 기존 버튼 위치까지 내려가기 전에 최고 속도에 도달하며, 그 아래에서도 선택 좌표는 마지막으로 보이는 미디어 행에 고정되어 스크롤 중 연속 선택을 이어간다.
- 상단 자동 스크롤의 기존 속도와 단순 버튼 탭은 변경하지 않았다. 상·하단 최대 속도 대칭과 하단 좌표 제한을 정책 시험으로 고정했다.
- SM-F968N에서 하향 드래그를 유지하는 동안 하단 5개 버튼이 모두 사라지고 자동 스크롤과 함께 선택이 39개까지 연속 증가했다. 손을 떼면 39개 선택을 유지한 채 버튼이 즉시 복원됐으며, 상단 취소로 선택을 폐기하고 추가·저장·제작은 실행하지 않았다.

## 2026-08-14 GitHub 원터치 업데이트 · Android 빌드 544

- Android 전용 배포 경로로 공식 `armsone/HanClip-Android`의 최신 안정 GitHub Release를 앱 시작 시 한 번 확인한다. 현재 빌드보다 높은 `android-vN`과 정확한 `HanClip-Android-vN.apk`만 안내하며 사용자가 `업데이트`를 눌러야 다운로드한다.
- 다운로드 후 HTTPS 저장소 경로·크기·package name·versionCode·현재 설치본 인증서를 모두 확인하고, 다시 사용자가 선택해야 Android 설치 화면을 연다. 자동 설치하지 않으며 영화·컬렉션·설정 저장소는 변경하지 않는다.
- iOS에는 없는 Android 직접 설치 배포 방식이다. 실제 배포는 소스 push가 아니라 `docs/GITHUB_UPDATES.md` 규칙에 맞는 안정 Release APK 공개가 필요하다.
- 전체 JVM 시험, `assembleDebug`, `assembleDebugAndroidTest`, `lintDebug`, `assembleReleaseQa`가 통과했다. 디버그와 releaseQa의 SHA-256 인증서가 일치하고 releaseQa의 package/version/설치 권한을 확인했다. SM-F968N과 Android 10 SM-T500에 releaseQa 544를 데이터 보존 설치해 실행했으며 두 기기에서 안정 Release JSON 시험 2건씩 통과했다.

## 2026-08-15 매치업 홈 카탈로그·테마 알림

- 매치업 스킬 기준으로 iOS 호출 화면 23종을 다시 목록화하고 Android phone의 홈 기본·미디어 메뉴·테마 탭·카피라이터·fontScale 2.0 상태를 고정 캡처했다.
- 최신 iOS Swift와 Android가 프리셋 높이 `74 + 72`, 3/2/1열, 내부 3등분, 설명 10.4 크기를 공유함을 확인했다. 기존 iPhone PNG는 최신 타이포 변경 전 자료이므로 구조 참고용으로만 기록했다.
- Android에서 빠져 있던 로고 짧은 탭의 `“<테마>로 변경했습니다.”` 2초 캡슐을 iOS와 동일한 문구·위치·크기로 복원했다.
- 최신 iOS 앱의 새 시뮬레이터 빌드와 나머지 화면의 양쪽 자동 캡처는 읽기 전용 정책 및 현재 캡처 자산 범위 때문에 후속 매치업으로 남긴다.
- 퀵모드 실제 상태를 1.0/1.3배에서 재캡처해 1.3배 첫 진입 때 만들기 CTA가 내비게이션 아래로 절반 잘리는 회귀를 발견했다. CTA를 고정 하단 구조로 복원하고 시스템 내비게이션 inset의 에뮬레이터 편차를 포함하는 안전 여백과 목록 하단 보상 공간을 분리했다. 첫 진입에서는 CTA 전체, 아래로 스크롤한 상태에서는 화면 비율 6개 전체와 CTA를 함께 확인했다.
- 사진 선택은 에뮬레이터의 한 제스처에서 건너뛴 셀까지 8개를 선택한 뒤 손가락을 반대로 되돌렸을 때 앵커부터 현재 위치까지 3개만 남는 중간 상태를 캡처했다. 드래그 중 하단 버튼 행이 숨겨지는 것도 두 상태에서 확인했고 `MediaDragSelectionPolicyTest` 전체가 통과했다.
- Android 빌드 545 배포 후보에서 전체 JVM 시험, `assembleDebug`, `assembleDebugAndroidTest`, `lintDebug`, `assembleReleaseQa`가 통과했다. 기존 544 설치본과 인증서가 일치하는 releaseQa를 `SM-F968N`에 데이터 보존 방식으로 업데이트했고, 545 실행과 영화 목록 2개·컬렉션 4개 보존을 확인했다.
